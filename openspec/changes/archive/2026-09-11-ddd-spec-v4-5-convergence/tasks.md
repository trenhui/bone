## 1. M1 规范文档重构（仅改文档，不动代码）

- [x] 1.1 在规范文首新增「一页纸速览」：v4.5 铁律 8 条 + L0–L3 能力开关表 + 按角色阅读路径（新人 / 评审 / AI）｜验证：未读过本规范的人可在 5 分钟内读完并复述 8 条铁律。**落地**：文档 L20 起「一页纸速览（v4.5）」含 R1-R8 铁律表（L26-48 每行带判定入口）＋ L0-L3 能力开关表 + 按角色阅读路径
- [x] 1.2 统一编号为 `P-`（原则）/ `E-`（工程）/ `G-`（门禁）三段，消除「第一部分 §10」与「第二部分 §10」的双编号歧义｜验证：全文档无重复编号，所有锚点链接可解析。**落地**：L11 明确「文档三段编号 P-/E-/G-」，全文档锚点（如 `#p-55-...`、`#e-53-...`、`#g-1-...`）可解析
- [x] 1.3 D2-1：把 §17 的 D0/D1/D2 改述为「可持久化充血模型（Active Record 风格聚合）」，D0 只留给无表的值对象与领域服务，并写明 PO 分离退出条件（出现第二消费者 / schema 与模型分叉）｜验证：文中不再出现「领域纯净度分级」表述。**落地**：E-8「领域模型与纯净度 D0/D1/D2」改口为「可持久化充血模型」，L11 摘要明示
- [x] 1.4 D2-2：§10.2 改述为「厚共享内核的模块化单体」，并新增共享内核依赖白名单（允许 `ApiResponse`、`BizException`、`TenantContext` 等）｜验证：上下文图标注「已落地 / 规划中」两层。**落地**：P-10「Bone 上下文映射参考」承认「厚共享内核的模块化单体」，L11 摘要明示
- [x] 1.5 D2-3：§14.4 把 `bone-gateway` 从「应用 / 控制面 BFF」改为「基础设施服务，豁免 §14」｜验证：与 §10.2「纯路由、无业务语义」不再矛盾。**落地**：E-5.4「模块适用性按性质而非物理位置」将 gateway 归为基础设施服务，L11 摘要明示
- [x] 1.6 D4：删除 Facade 触发条件 F3（「>7 个 Handler」魔法数），仅保留 F1（多入站适配器）与 F2（对外 Client SDK）｜验证：§14.3.2 与 §23.2 中无 F3 残留。**落地**：E-5.3.2 L651「原 F3 已删除——数量是风格问题」
- [x] 1.7 D4：`*Orchestrator` 由 ADR 例外降为模块 README 三级例外（需写明步骤 / 一致性 / 失败补偿）｜验证：文中给出 README 登记模板。**落地**：E-5.3 L602-610 模块 README 例外 + L379/E-0.4 登记模板（含可重试编排语义）
- [x] 1.8 D4：允许 `{语义}ApplicationService`（如 `CheckoutApplicationService`）放 `application/orchestration/`；修复 §14.3.1 与 §23.2 的 Service 矛盾；落命名黑名单（`*Manager`、`Common*`、`Base*`、`Business*`）与行为规则（不得操作仓储、不得改聚合状态）｜验证：全文对 Application Service 的描述前后一致。**落地**：E-5.3.1 L628「{语义}ApplicationService」例外 + 命名黑名单；L638 保留硬约束；与 2.6 规则命名黑名单一致（`Common*`/`Base*`/`Business*`/`*Manager`）
- [x] 1.9 D5：读路径收敛为「写模型加载走 Repository / 查询走 QueryHandler」两条 + `*ReadPort` 例外（仅 QueryBuilder 物理做不到时）；明确 CommandHandler 可注入 ReadPort 做写前唯一性校验；简单 CRUD 的 QueryHandler 允许直接 `findById`｜验证：§18.5 决策树只剩三个分支且含「禁止作为仓储泄压阀」约束。**落地**：L894「读路径决策树（两条 + *ReadPort 例外）」
- [x] 1.10 D6：用「按可靠性触发」的决策表替换 §5.4「Outbox（可选）」，并补齐失败语义（save 成功 publish 失败、AFTER_COMMIT 订阅失败、无 Outbox 双写）｜验证：文中明确「跨进程发布必须 Outbox」。**落地**：P-5.4 决策表 L168-169（按可靠性触发），L11 摘要明示
- [x] 1.11 D7：异常收敛为 `DomainException` / `BizException` / `InfrastructureException` 三个根，其余降级为子类或错误码；登记 `ApiResponse.success(String)` 单参重载的 API 改造项（不靠注释当门禁）｜验证：§16.3 异常表只剩三个根类条目。**落地**：E-7.3「异常与 API 规范」收敛三根（L11 摘要）
- [x] 1.12 D8：多租户参数化——租户作显式参数进入仓储方法；`TenantContext` 只在适配层解析一次；Job / MQ 消费 / AFTER_COMMIT 订阅等异步路径禁止依赖 ThreadLocal 传租户；超管用独立端口；补「领域概念 vs 数据隔离」判定规则｜验证：§13.4 不再要求「优先继承 TenantAggregateRoot」。**落地**：E-4.4「多租户在 DDD 中的约定」（L11 摘要）
- [x] 1.13 D13：§14.4 增加「CRUD 支撑域豁免档」——纯 CRUD、无演进压力的支撑域允许 Transaction Script + 表实体，不强制 Handler / 聚合 / ReadPort 三件套｜验证：给出豁免模块判定标准与登记方式。**落地**：E-5.4「CRUD 支撑域豁免」（L11 摘要）
- [x] 1.14 把 §5.5 支付样板外置到 `doc/architecture/ddd/samples/payment.md`，原则部分只保留一句硬约束（幂等在聚合内 + 生产必须有存储级兜底）｜验证：主文档不再包含状态机、HMAC、5 分钟 Job 等支付细节。**落地**：P-5.5 主文档仅留两条硬约束（L178-181），完整样板已外置 `doc/architecture/ddd/samples/payment.md`（状态机表、幂等键、存储级兜底、含失败回调的验签端口、5 分钟 Job、生产补齐清单），消除原断链
- [x] 1.15 删除 COLA 术语对照（§23.1 注脚与 §14.3.2 对照段）｜验证：文中无 COLA 对照内容
- [x] 1.16 freeze 台账模板增加「拆除条件 + 到期目标 + 上次违规数」字段，并规定违规数单调不增｜验证：附录 B.3 模板含三列且说明 CI 校验方式
- [x] 1.17 同步交叉引用：`AGENTS.md` 第 11 节、`doc/architecture/ddd/` 分册、`adr/0011`、`0012`、`0013`、各模块 README 例外表、`bone-architecture-test/README.md`｜验证：全库搜索旧章节号无失效引用
- [x] 1.18 更新 §22.2 AI / Agentic 生成守则：先判断一致性边界，再生成结构；移除对后缀与包路径的过度强调｜验证：守则条目可逐条对照新铁律

## 2. M2 门禁重配（动 `bone-architecture-test`）

- [x] 2.1 D3：改造 `domainRepositoriesShouldOnlyDeclareWhitelistedMethods()`，判定从「方法名匹配」改为「返回类型必须是聚合根 / `Optional<聚合根>` / `boolean` / `void`」，允许复合自然键，禁用持久化词汇入方法名。**API 细节**：ArchUnit 1.2.1 中 `getMethods()` 即声明方法（`getAllMethods()` 才含继承）；`JavaMethod.getReturnType()` 返回 `JavaType`（无 `getSimpleName()`），泛型实参须经 `JavaParameterizedType.getActualTypeArguments()` 获取。**单元测试放在 bone-core 测试树**（规则库被 bone-core test 依赖，反向加依赖会成环）｜验证：`DomainRepositoryReturnTypeRuleTest` 2 用例全绿（合法仓储复合键 + `Optional<聚合>` 不误报；DTO / `List<聚合>` / 持久化词汇被拦截）；`bone-blueprint` 137 测试全绿；其余 7 个应用模块 `ArchitectureTest` 全绿——唯一存量违规 `bone-extension-studio` 的 `StudioAuditRepository.save` 返回非 Entity 的 POJO，已修为 `void`（审计 append-only，调用方不依赖返回值）
- [x] 2.2 D9：跨上下文规则改为全限定名正则匹配（排除自身模块），去掉 `allowEmptyShould(true)`，空匹配视为配置错误｜验证：在 `bone-blueprint` 上跑，对已知越界引用能稳定命中。**实现**：`BoneDddArchRules.noCrossContextDomainDependency(selfRootPackage, sharedKernelPackages...)`——按依赖目标 FQN 前缀匹配字节码引用（与 classpath 无关），排除自身模块根包与共享内核（默认 `com.bone.core`、`com.bone.metadata.sdk`，后者因 blueprint 聚合使用 `@Table` 注解而内置）；**不加 `allowEmptyShould(true)`**。**API 事实**：ArchUnit 1.2.1 对空匹配（`that()` 无命中类）在 `evaluate` 时直接抛 `AssertionError`（"failed to check any classes"），即「配置错误」信号。**验证**：`NoCrossContextDomainDependencyRuleTest` 3 用例全绿（越界依赖被拦截且共享内核不误报 / 仅依赖共享内核合法 / 空匹配抛 AssertionError）；blueprint 接入后 `no_cross_context_domain` 0 违规（134 测试全绿）
- [x] 2.3 D1：聚合纯单测门禁——最终落地为 **`AggregatePureUnitTestGuard.verify("<根包>")`**（`bone-architecture-test`）。**为何不是 `ArchRule`**：ArchUnit 的 `@AnalyzeClasses` 默认带 `ImportOption.DoNotIncludeTests`，`ArchitectureTest` 看不到测试类，无法判定「某聚合是否存在对应纯单测」；故改用 `ClassFileImporter` 分别导入主代码（`DoNotIncludeTests`）与测试代码（`OnlyIncludeTests`）两个类集合交叉比对，由模块内**独立测试类**承载。判据三条全满足才算通过：① 存在同名 `*Test` 类；② 至少 1 个 `@Test` 方法（堵住空类绕过）；③ 无容器注解（`@SpringBootTest` / `@DataJpaTest` / `@ExtendWith(SpringExtension.class)` 等）。仅统计 `..domain..` 下**具体**聚合根（排除抽象 / 接口 / 基础设施持久化记录）｜验证：`bone-blueprint` 接入 `AggregatePureUnitTestCoverageTest` 后 **137 测试全绿**；**负向对照**已做——临时新增无测试的探针聚合，守卫准确报出 `TempProbeAggregate：缺少同名纯单测类 TempProbeAggregateTest`，且未误报已有测试的 `Order` / `Payment`
- [x] 2.4 为 `bone-blueprint` 现有聚合补齐纯单测（主状态机 + 至少一条拒绝路径，无容器 / 无 DB / 无 MQ）｜验证：新增测试可在纯 JUnit 下运行，不启动 Spring 上下文。**结论**：`OrderTest`（18 用例：下单→支付→发货→送达→退款主状态机 + 空商品 / 金额不匹配 / 非法迁移 / 重复退款拒绝路径）与 `PaymentTest`（17 用例：pending→paying→success/failed/closed→refund + 幂等 / 重复回调拒绝）已完备，纯 JUnit 无容器注解，直接通过 `AggregatePureUnitTestCoverageTest` 门禁；无需新增
- [x] 2.5 降级：把 `commandHandlersShouldBeNamedCommandHandler`、`queryHandlersShouldBeNamedQueryHandler`、`commandHandlersShouldBeTransactional`、`queryHandlersShouldBeReadOnlyTransactional` 移出硬门禁，改为 Checkstyle warn 或 CI 提示｜验证：`ArchitectureTest` 中这四条不再阻断构建。**落地**：规则方法保留在 `BoneDddArchRules`（Javadoc 标注「v4.5 已降级为 warn」供 CR / 手动检查），8 个应用模块 `ArchitectureTest` 全部移除这四条 `@ArchTest`；反贫血主判据切换为 R8 聚合纯单测（2.3）
- [x] 2.6 改造 `adapterControllersMustNotDependOnApplicationService()` 为「禁 `application/service` 包 + 命名黑名单」判定｜验证：`..application.facade..` 与 `..application.orchestration..` 不被误伤。**实现**：`resideInAPackage("..application.service..")` ∪（`resideInAPackage("..application..")` ∩ 命名黑名单 `Common*`/`Base*`/`Business*`/`*Manager`）。**API 事实**：`ClassesShould` 无 `notDependOnClassesThat(DescribedPredicate)`，须用 `noClasses()...should().dependOnClassesThat(predicate)`；`ClassesThat`/`ClassesShould` 链式位用 `haveSimpleNameEndingWith/StartingWith`，`JavaClass.Predicates` 静态谓词（`simpleNameEndingWith` 等）仅用于 `dependOnClassesThat(...)` 传参位。**验证**：`AdapterControllerDependencyRuleTest` 2 用例全绿（`LegalController` 仅依赖 facade/orchestration 无违规；`OrderController` 依赖 service 实现 + Manager 被拦截）。**教训**：不能靠「违规消息 contains 类名」断言不误伤——违规消息会整行写入构造函数签名（含全部参数类型），须拆分合法/违规两个独立夹具类
- [x] 2.7 更新各应用模块 `ArchitectureTest` 与 `archunit_store/` 基线（`bone-blueprint` 保持 0 违规；其余模块按新规则 freeze 存量）｜验证：基线文件提交入库且违规数不超过上次记录。**落地**：8 个应用模块 `ArchitectureTest` 统一移除 2.5 四条降级规则并接入 `no_cross_context_domain`（模块根包：iam/masterdata/integration/system/`platform.alert`/`studio.generator`/`metadata.catalog`/`engine.extension.studio`），全部 0 违规全绿（无存量越界引用，无需 freeze）；`archunit_store` 由 FreezingArchRule 运行刷新（被移除规则的孤儿条目待清理，见遗留）
- [x] 2.8 全量验证：`mvn clean install` 全绿，强制规则数由 20 条收敛至约 13 条 + 4 条 warn｜验证：构建日志显示 BUILD SUCCESS 且规则清单符合预期。**结果**：`mvn clean install -Djacoco.skip=true` 全库构建，blueprint 134 / iam / masterdata 31 / integration / notification / metadata-server / studio-generator / extension-studio 全部 SUCCESS（各模块 ArchitectureTest 全绿，`no_cross_context_domain` 0 违规）；**规则清单**：`BoneDddArchRules` 共 21 个 public 规则方法 = **17 强制 + 4 warn**（2.5 降级四条已移出门禁；「约 13」为设计估算，实际保留 `noNewDomainStorePackage` 等有效规则故为 17）。**已知环境遗留**：bone-system 3 个 E2E 测试（`ConsoleSecurityTest` 等）因本地测试库缺 `sys_schedule_task` 表报错（DDL 已在 `bone-init.sql`，重导库即恢复；与本 change 规则无关）；另修复 masterdata 预存编译错误 `DeleteDataStandardCommand` 缺全参构造器（补 `@AllArgsConstructor`）

## 3. M3 前置 ADR 与 spike（本阶段只出草稿）

- [x] 3.1 确认 ADR-0019 可行性（替代原独立 spike）：实测 `BaseRepository.insert()` 第 164-167 行无条件 `generateId + setEntityId`，而 `batchInsert`（196）/ `ensureIdInitialized`（658）已 `if (id == null)`；全平台 insert 直接调用点仅 20 处（masterdata 3 / extension-studio 9 / studio-generator 2 / metadata-server 3），零「预置 id 依赖覆盖」反模式，主键策略全部 `DISTRIBUTED_ID` → **可行性已确认、低风险**；产出影响面清单文档（无需独立 spike）｜验证：清单含调用点计数、反模式核查结论、主键策略分布三项证据
- [x] 3.2 起草 ADR-0019（已产出 `doc/architecture/adr/0019-id-generation-contract-respect-non-null-id.md`）：ID 生成契约——`BaseRepository.insert()` 改为「仅 id 为空时生成（与 `batchInsert`/`ensureIdInitialized` 一致）」，尊重调用方预置的非空 id；**必须在 ADR 中写明 ADR-0019 是「身份在构造期确定」的先决条件（非可选第二步）**，并写清现状「应用层预分配 id 会被 `insert` 静默替换」及 blueprint 明细外键隐患；明确**本 change 只出 ADR 草稿，SDK 代码改动延后至执行该 ADR 的后续 change**｜验证：ADR 含动机、影响模块（20 处 insert 调用方）、迁移路径（单点改 `insert` 分支）、回滚（还原 `insert` 即回退）
- [x] 3.3 起草 ADR-0020（已产出 `doc/architecture/adr/0020-anti-anemia-weak-form-strong-behavior.md`）：反贫血机制按「弱约束代码形式、强约束行为」收敛——(1) P2 聚合纯单测（R1）为主判据、(2) 身份早期确定由 D11 固化、(3) ORM 恢复走 SDK 字段级反射（不要求业务层 `reconstitute()`）、(4) `outerLayersMustNotMutateAggregateIdentity()` 维持风格约束（非硬门禁）、(5) 基类去 `@Data` 分阶段（仅 `studio-generator` 约 5 个 domain 类）；**不再产出「强制 `reconstitute()`」类 ADR**｜验证：ADR 含现状实测（基类已 `@Getter`、SDK 走 `ReflectionUtil.setFieldValue`、studio-generator `@Data` 清单）与分批收敛方案
- [x] 3.4 起草 ADR-0021（已产出 `doc/architecture/adr/0021-outbox-and-consumer-idempotency-platformization.md`）：Outbox 与消费端 `eventId` 幂等平台化（事件 id、投递重试、死信、幂等表作为平台原语）｜验证：ADR 明确平台组件边界与模块接入方式
- [x] 3.5 起草 ADR-0022（已产出 `doc/architecture/adr/0022-external-callback-signature-verification-port.md`）：外部回调验签端口化（由支付样板推广为全局硬规则）｜验证：ADR 含端口定义与验签失败处理约定
- [ ] 3.6 【阻塞于 ADR-0019】在 `bone-blueprint` 固化 D11 模式（不新增端口）：ADR-0019 落地**后**再固化「`DistributedIdGenerator.generateLongId()` 生成 id 并作为聚合构造参数传入」，并新增断言测试锁定「构造期 id == 持久化后 id」不变量。⚠️ **ADR-0019 落地前不得断言该 id「稳定」**——当前 `insert` 会静默替换，blueprint 现有写法属超前于 SDK 能力的待对齐项；本期不触及明细持久化｜验证：ADR-0019 已落地；blueprint 既有 136 测试仍全绿；新增锁定测试在纯 JUnit 下通过

## 4. 验证与收尾

- [x] 4.1 铁律判定映射表：8 条铁律逐条标注判定方式（ArchUnit / Checkstyle / CR）与对应规则名｜验证：每条铁律至少有一个可执行的判定入口。**落地**：「一页纸速览」R1-R8 表每行含「判定入口」列（如 R7 应用层单层 → ArchUnit `noUseCaseClassesInApplication`/`adapterControllersMustNotDependOnApplicationService` 等）；G-1 规则清单（约 L964）逐条列出 ArchRule 名与约束编号
- [x] 4.2 一致性检查：核对文档内部矛盾清单（含 §14.3.1 ↔ §23.2、§10.2 ↔ §14.4、§5.4 ↔ §5.5）全部消除｜验证：矛盾清单逐条勾选为已解决。**落地**：§14.3.1↔§23.2 由 E-5.3 统一（ApplicationService 命名黑名单 + 硬约束 L638）；§10.2↔§14.4 由 E-5.4 gateway 归基础设施服务统一；§5.4↔§5.5 由 P-5.4/P-5.5 决策表 + 支付样板外置统一；L11 v4.5 收敛摘要三处公开改口均对应
- [ ] 4.3 试读验证：请一位未参与本 change 的同学按「一页纸速览」阅读并复述 8 条铁律与 L0–L3 分级｜验证：能正确复述铁律条目与自身模块所处的复杂度档位。⚠️ 需人工执行：由未参与本 change 的同学试读「一页纸速览」（Bone-DDD 最终实践方案 L20 起）并复述，完成后勾选
