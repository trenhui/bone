package ${basePackage}.${table.moduleName}.domain.repository;

import com.bone.core.domain.BaseRepository;
import ${basePackage}.${table.moduleName}.domain.model.${table.className};
import org.springframework.data.jdbc.repository.query.Query;
import org.springframework.data.repository.query.Param;
import org.springframework.stereotype.Repository;

import java.util.List;

/**
 * ${table.classComment}
 * @author ${table.author}
 */
@Repository
public interface ${table.className}Repository extends BaseRepository<${table.className},Long>{

}
