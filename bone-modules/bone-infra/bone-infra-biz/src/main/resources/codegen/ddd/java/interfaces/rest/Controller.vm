package ${basePackage}.${table.moduleName}.interfaces.rest;

import com.bone.core.result.PageResult;
import com.bone.core.result.Result;
import ${basePackage}.${table.moduleName}.domain.model.${table.className};
import ${basePackage}.${table.moduleName}.domain.service.${table.className}Service;
import ${basePackage}.${table.moduleName}.interfaces.convert.${table.className}Convert;
import ${basePackage}.${table.moduleName}.interfaces.dto.${table.className}DTO;
import io.swagger.v3.oas.annotations.Operation;
import io.swagger.v3.oas.annotations.Parameter;
import io.swagger.v3.oas.annotations.Parameters;
import io.swagger.v3.oas.annotations.enums.ParameterIn;
import io.swagger.v3.oas.annotations.enums.ParameterStyle;
import io.swagger.v3.oas.annotations.tags.Tag;
import jakarta.annotation.Resource;

import jakarta.servlet.http.HttpServletResponse;
import jakarta.validation.Valid;
import org.springframework.data.domain.Pageable;
import org.springframework.data.web.PageableDefault;
import org.springframework.security.access.prepost.PreAuthorize;
import org.springframework.validation.annotation.Validated;
import org.springframework.web.bind.annotation.*;

import java.io.IOException;
import java.util.Collection;
import java.util.List;

/**
* ${table.classComment}DTO
*
* @author ${table.author}
*/
@Tag(name = "系统 - ${table.classComment}管理", description = "系统 - ${table.classComment}管理")
@RestController
@RequestMapping("/${table.moduleName}/${classNameVar}")
@Validated
public class ${table.className}Controller {
    @Resource
    private ${table.className}Service ${classNameVar}Service;

    @Resource
    private ${table.className}Convert ${classNameVar}Convert;

    @PostMapping("/create")
    @Operation(summary = "创建${table.classComment}")
    @Parameter(name = "${classNameVar}DTO", description = "${table.classComment}信息", required = true)
    @PreAuthorize("@ss.hasPermission('${table.moduleName}:${classNameVar}:create')")
    public Result<Long> create(@Valid @RequestBody ${table.className}DTO ${classNameVar}DTO) {
        ${table.className} ${classNameVar} = ${classNameVar}Convert.toEntity(${classNameVar}DTO);
        return Result.ok(${classNameVar}Service.create(${classNameVar}).getId());
    }

    @PutMapping("/update")
    @Operation(summary = "修改${table.classComment}")
    @Parameter(name = "${classNameVar}DTO", description = "${table.classComment}信息", required = true)
    @PreAuthorize("@ss.hasPermission('${table.moduleName}:${classNameVar}:update')")
    public Result<Boolean> update(@Valid @RequestBody ${table.className}DTO ${classNameVar}DTO) {
        ${table.className} ${classNameVar} = ${classNameVar}Convert.toEntity(${classNameVar}DTO);
        ${classNameVar}Service.update(${classNameVar});
        return Result.ok();
    }

    @DeleteMapping("/delete/{id}")
    @Operation(summary = "删除系统${table.classComment}")
    @Parameter(name = "id", description = "主键id", required = true, in = ParameterIn.PATH, style = ParameterStyle.SIMPLE)
    @PreAuthorize("@ss.hasPermission('${table.moduleName}:${classNameVar}:delete')")
    public Result<Boolean> delete(@PathVariable("id") Long id) {
        ${classNameVar}Service.delete(id);
        return Result.ok();
    }

    /**
     * 批量删除
     *
     * @param ids
     * @return
     */
    @Operation(summary = "批量删除")
    @PreAuthorize("@ss.hasPermission('${table.moduleName}:${classNameVar}:batchDelete')")
    @Parameter(name = "ids", description = "主键id列表", required = true, in = ParameterIn.QUERY, example = "1024,2048", style = ParameterStyle.SIMPLE)
    @DeleteMapping(value = "/batchDelete")
    public Result<String> batchDelete(@RequestParam(name = "ids", required = true) Collection<Long> ids) {
        this.${classNameVar}Service.batchDelete(ids);
        return Result.ok("批量删除成功!");
    }

    @GetMapping("/get/{id}")
    @Operation(summary = "获取${table.classComment}")
    @Parameter(name = "id", description = "主键id", required = true, in = ParameterIn.PATH, style = ParameterStyle.SIMPLE)
    @PreAuthorize("@ss.hasPermission('${table.moduleName}:${classNameVar}:query')")
    public Result<${table.className}DTO> get(@PathVariable("id") Long id) {
        ${table.className} ${classNameVar} = ${classNameVar}Service.get(id);
        return Result.ok(${classNameVar}Convert.toDTO(${classNameVar}));
    }

    @GetMapping("/list")
    @Operation(summary = "列表查询${table.classComment}")
    @Parameter(name = "ids", description = "主键id列表", required = true, in = ParameterIn.QUERY, example = "1024,2048", style = ParameterStyle.SIMPLE)
    @PreAuthorize("@ss.hasPermission('${table.moduleName}:${classNameVar}:query')")
    public Result<List<${table.className}DTO>> list(@RequestParam(name = "ids", required = true) Collection<Long> ids) {
        List<${table.className}> list = ${classNameVar}Service.list(ids);
        return Result.ok(${classNameVar}Convert.toDTOList(list));
    }

    /**
     * 分页查询
     * @param${classNameVar}DTO 查询对象
     * @param pageable 注传参格式： size=10&page=0 &sort=id,asc&sort=name,desc
     * @return
     */
    @GetMapping("/page")
    @Operation(summary = "分页查询${table.classComment}")
    @Parameters({
            @Parameter(name = "${classNameVar}DTO", description = "查询对象", required = false),
            @Parameter(name = "pageable", description = "前页参数", required = true)
    })
    @PreAuthorize("@ss.hasPermission('${table.moduleName}:${classNameVar}:query')")
    public Result<PageResult<${table.className}DTO>> pageable(${table.className}DTO ${classNameVar}DTO, @PageableDefault(size = 15) Pageable pageable) {
        ${table.className} ${classNameVar} = ${classNameVar}Convert.toEntity(${classNameVar}DTO);
        PageResult<${table.className}> pageResult = ${classNameVar}Service.page(${classNameVar}, pageable);
        return Result.ok(${classNameVar}Convert.toPageResult(pageResult));
    }

    @GetMapping("/export")
    @Operation(summary = "导出${table.classComment}Excel")
    @Parameter(name = "${classNameVar}DTO", description = "查询参数", required = true)
    @Parameter(name = "response", description = "HttpServletResponse", required = true)
    @PreAuthorize("@ss.hasPermission('${table.moduleName}:${classNameVar}:export')")
    public void export(${table.className}DTO ${classNameVar}DTO,
                       HttpServletResponse response) throws IOException {
        ${table.className} ${classNameVar} = ${classNameVar}Convert.toEntity(${classNameVar}DTO);
        List<${table.className}> list = ${classNameVar}Service.list(${classNameVar});
        List<${table.className}DTO> result = ${classNameVar}Convert.toDTOList(list);
        // 导出 Excel
        //List<${table.className}DTO> datas = ${table.className}Convert.toDTOList(list);
        //ExcelUtils.write(response, "${table.className}.xls", "数据", ${table.className}ExcelDTO.class, datas);
    }
}
