package ${basePackage}.${table.moduleName}.interfaces.convert;

import com.bone.core.result.PageResult;
import ${basePackage}.${table.moduleName}.domain.model.${table.className};
import ${basePackage}.${table.moduleName}.interfaces.dto.${table.className}DTO;
import org.mapstruct.Builder;
import org.mapstruct.Mapper;
import org.mapstruct.MappingConstants;

import java.util.List;

/**
 * ${table.classComment} Convert
 *
 * @author ${table.author}
 */
@Mapper(builder = @Builder(disableBuilder = true),componentModel = MappingConstants.ComponentModel.SPRING)
public interface ${table.className}Convert {
    /**
     * 转换成Entity
     *
     * @param dto DTO对象
     * @return
     */
    ${table.className} toEntity(${table.className}DTO dto);

    /**
     * 转换成DTO
     *
     * @param entity 实体
     * @return
     */
    ${table.className}DTO toDTO(${table.className} entity);

    /**
     * toDTOList
     *
     * @param list
     * @return
     */
    List<${table.className}DTO> toDTOList(List<${table.className}> list);

    /**
     * toEntityList
     *
     * @param list
     * @return
     */
    List<${table.className}> toEntityList(List<${table.className}DTO> list);

    /**
     * toPageResult
     *
     * @param pageResult
     * @return
     */
    PageResult<${table.className}DTO> toPageResult(PageResult<${table.className}> pageResult);
}
