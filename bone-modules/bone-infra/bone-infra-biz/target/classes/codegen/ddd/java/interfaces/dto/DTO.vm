package ${basePackage}.${table.moduleName}.interfaces.dto;

import com.bone.core.tenant.TenantAbstractDTO;
import io.swagger.v3.oas.annotations.media.Schema;
import lombok.*;
#foreach($column in $columns)
 #if(!${baseDOFields.contains(${column.javaField})})
   #if(${column.javaType.toLowerCase()} == "localdatetime")
import java.time.LocalDateTime;
   #break;
   #end
 #end
#end
#foreach($column in $columns)
 #if(!${baseDOFields.contains(${column.javaField})})
   #if(${column.javaType.toLowerCase()} == "date")
import java.util.Date;
   #break;
   #end
 #end
#end
#foreach($column in $columns)
 #if(!${baseDOFields.contains(${column.javaField})})
    #if (${column.javaType} == "BigDecimal")
import java.math.BigDecimal;
   #break;
   #end
 #end
#end

/**
* ${table.classComment}DTO
*
* @author ${table.author}
*/
@Data
@Schema(description = "${table.classComment}DTO")
@EqualsAndHashCode(callSuper = true)
@ToString(callSuper = true)
public class ${table.className}DTO extends TenantAbstractDTO<Long> {

    #foreach ($column in $columns)
    #if (!${baseDOFields.contains(${column.javaField})})##排除 BaseDO 的字段
        /**
         * ${column.columnComment}
        #if ("$!column.dictType" != "")##处理枚举值
         *
         * 枚举 {@link TODO ${column.dictType} 对应的类}
        #end
         */
         @Schema(name = "${column.columnComment}")
        private ${column.javaType} ${column.javaField};
    #end
    #end
}
