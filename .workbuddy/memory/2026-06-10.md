
## 2026-06-10 UI 规范深化

### toAntdTheme 引用检查
- 全部 8 个微应用均通过 `BoneAppProvider` / `createBoneMicroAppRenderer` 间接使用 `toAntdTheme`，架构正确

### 交互规范审查与修复 (bone-shell-spec v4 → v5)
**P0 修复**:
- IAM 管理：移除与侧边栏子菜单冗余的页内 Tab 栏，改为侧边栏子菜单独立路由；全局菜单结构表 IAM 交互模式从「Tab 模式」改为「侧边子菜单（独立路由）」
- JS bug：`toggleMenuGroup` 展开时仅在无激活子项时才自动导航到第一个子项，避免关闭再展开时强制跳转

**P1 修复**:
- Phase 2 页面加载顺序：骨架屏修正为路由匹配后立即渲染（在加载微应用之前），最终步骤改为「骨架屏 → 真实内容过渡」
- Phase 4 离开页面：补充未保存变更检测 → ConfirmModal → 确认后才销毁
- 新增 Section 6 键盘无障碍规范：Tab/Shift+Tab、Enter 提交、Esc 关闭、Focus Trap、全局快捷键 Ctrl+K/S/F、↑↓ 列表导航
- 部署管理：更新中行全部操作链接置灰 + cursor:not-allowed

**P2 优化**:
- 搜索+筛选：明确 300ms 防抖
- 创建弹窗：补充 button loading spinner 和提交失败保留弹窗规则
- 详情抽屉：补充关闭时脏检测确认
- 页内 Tab：明确禁止与侧边栏子菜单同时存在的双重导航
- 链路：`toAntdTheme` → `BoneAppProvider` → `ConfigProvider`（所有应用共享），无需应用单独引入

### App.css 迁移至 CSS 变量
- `bone-shell/src/App.css` 全面迁移：硬编码颜色 → `--bone-color-*` CSS 变量
- 移除所有 `.dark` 选择器硬编码覆写（共删除 20+ 处暗色覆写规则）
- 亮/暗主题自动切换由 `applyThemeCss` 注入的 CSS 变量驱动，无需 JS 类名切换

### 产出可视文档
- **theme-demo.html**: 交互式组件规范演示（按钮/表单/表格/弹窗 + 亮暗实时切换）
- **bone-shell-spec.html**: 主体框架视觉规范（侧边栏/顶栏/布局模式/菜单结构/尺寸规范）

### 8 项菜单页面视觉稿与交互规范
- 基于 PRD v2.1 为全部 8 个菜单项设计了完整页面布局和交互模式
- 核心交互优化：
  - **IAM**：顶部 Tab（用户/角色/权限/审计），避免二级菜单跳转，同页快速切换
  - **元数据**：实体列表 → 行点击展开 Drawer 详情（字段/关系/校验/版本）
  - **集成**：连接器卡片 + 流程编排 + 场景化模板
  - **扩展**：扩展点/插件(JAR)/执行日志三 Tab
  - **代码生成**：实体选择 → 模板选择 → 预览 → 下载
- 交付文件：`.workbuddy/bone-pages-spec.html`（交互式预览，8 页面自由切换 + 亮/暗主题）

### 基于模块详设文档全面更新 8 页视觉稿 (v3.5)
- 完整阅读 `doc/design/modules/` 下全部 11 个模块详设文档，提取 UI 交互细节
- 重大更新：
  - **模块1 Dashboard**：补全 JdbcKeyMetricsGateway COUNT 指标、服务健康状态 (UP/DOWN/UNKNOWN)、JVM 资源、30s 轮询机制
  - **模块6 IAM**：扩为 5 Tab (用户/角色/权限/审计/租户)；TenantController + TenantQuotaEnforcer；权限码 {domain}:{resource}:{action}；角色继承 (RoleHierarchyResolver)
  - **模块4 集成**：补全连接器类型 (REST/SOAP/JDBC/FTP/MQ)；X6 画布引用；LinearSyncFlowRuntime 线性流程图；执行监控
  - **模块5 扩展**：扩为 6 Tab (扩展点/插件/部署状态机/依赖图/插件市场/执行日志)；部署状态机可视化 (Uploaded→Active)；LRO 进度条；游标分页
  - **模块8 Generator**：重写为 4 Tab (数据源管理/代码生成/模板管理/生成历史)；Monaco Editor 编辑器 mock；CATALOG_SNAPSHOT 选实体；LRO 202+轮询
  - **模块7 系统**：新增 K8s 部署 Tab [Vision]；补全日志搜索/导出
- 所有页面新增 spec-panel（API 前缀/端口/前端/关键技术栈）
- 交付：`.workbuddy/bone-pages-spec.html` (v3.5, 约 1500 行)

### 首页仪表盘生成到 Ardot 画布
- 使用 ardot-design-assistant 工作流在画布中生成 BONE Dashboard 首页
- 设计系统：Data-Dense Dashboard 风格 + Analytics Dashboard 色板（Primary #1E40AF）+ Minimal Swiss 排版（Inter）
- 文件 ID：691732915303663，文件名："BONE Dashboard 首页仪表盘"
- 完整结构：侧边栏(240px) + 顶栏(56px) + 内容区(KPI卡片/快速操作/健康表/操作记录)
- capture_layout 零问题，capture_screenshot 视觉验证通过

### 清理 bone-pages-spec.html 开发者文案
- 删除了全部 8 个菜单页面的开发者向内容：
  - **spec-panel**：5 个 API/端口/权限/服务信息面板
  - **page-desc**：8 个技术实现细节段落
  - **interact-box**：3 个数据刷新/交互模式/API 流程图
  - 内联注释：[Target]、[Vision]、API 路径、技术术语（RoleHierarchyResolver、CATALOG_SNAPSHOT、LinearSyncFlowRuntime、LRO 等）
- 文件从 1087 行缩减到 988 行（-99 行）
- 备份：bone-pages-spec.html.bak

### bone-shell-spec.html v3 重大升级
- 从 891 行扩展到 1050+ 行，新增三大核心板块：
  1. **二级菜单交互规范**（6 项规则卡片：点击展开/手风琴/箭头动画/高亮子项/父级路由/缩进层级）
  2. **页面交互模式**（9 项通用契约：面包屑、Tab 切换、创建→弹窗、详情→抽屉、删除→确认框、搜索筛选、分页、导出、自动刷新）
  3. **系统管理交互案例**（全链路流程 + 4 子页面交互规格表 + 每个子页的具体操作细节）
- 交互式 Shell Demo 升级：
  - 侧边栏改为 4 个可展开菜单分组 + 4 个单层叶子节点
  - 系统管理默认展开，展示配置/监控/日志/部署 4 个子页面
  - 点击子页面右侧内容区实时切换，面包屑自动更新
  - 手风琴模式：展开一个分组时自动收起其他
  - 每个子页面有独立内部 Tab（配置页：基础设置/安全策略/通知设置；日志页：全部/登录/配置变更/删除）
- 新增全局菜单结构表：8 个一级菜单 × 类型(叶子/分组) × 子页面列表 × 交互模式

### bone-shell-spec v4 — 补充每个菜单点开后的页面级交互
- v3 只有系统管理的页面交互规格，v4 覆盖全部 8 个菜单页
- 新增 **Section 2「点击菜单—宏观交互流程」**（4 阶段管道：导航触发 → 页面加载 → 空闲交互 → 离开页面）
- 新增 **Section 3「各菜单·页面级交互详细规格」**（8 张 page-spec-card）：
  - 每个页面包含：交互流程图、用户可操作列表、系统行为列表、边界状态 badges、核心操作 chips
  - 首页：30s 轮询 + KPI 点击跳转 + 服务健康 Drawer
  - IAM：4 Tab 无刷切换 + 创建 Modal + 编辑 Drawer + 禁用 ConfirmModal + 权限树 Popover
  - 元数据：新建向导 3 步 + Drawer 分 Tab 懒加载 + 编辑态切换
  - 主数据：选择实体→动态搜索字段 + 批量导入进度条 + 导出异步下载
  - 集成：连接器类型卡片 + 测试连接即时反馈 + 流程编排画布
  - 系统管理：4 子页面分别详述表单脏检测/30s轮询/日志级别着色/部署二次确认
  - 扩展：上传→解析→部署状态机流转 + 插件市场安装
  - 代码生成：实体+模板选择→异步生成→进度轮询→预览+下载
- Shell Demo 升级：全部 8 个菜单的 20+ 子页面均填充真实交互内容
- 全局菜单结构表新增「核心操作」列

### bone-pages-spec v3.5 → v4 对齐 shell-spec v5
- 侧边栏从平铺 nav-item 改造为手风琴分组结构（IAM/集成/扩展/代码生成/系统 5 个 nav-group）
- 每个分组含 `nav-group-header`（箭头动画）+ `nav-group-body`（子菜单项列表）
- IAM 模板移除冗余顶部 Tab 栏，改为纯侧边栏子菜单独立路由
- JS 全面重写：
  - `switchPage(pageId, subId)` — 支持子页面参数，自动激活对应侧边栏项
  - `toggleMenuGroup()` — 手风琴展开/收起，仅在无激活子项时自动导航到第一个
  - `showSubContent()` — 独立子内容显示 + 懒加载模板
  - 离开页面脏检测 — `hasDirtyForm` + confirm 弹窗
  - 点击顶级 nav-item 时自动收起其他分组
- 文件从 988 行扩展到 ~1140 行
