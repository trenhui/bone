spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bone?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate.format_sql: true
  redis:
    host: localhost
    port: 6379
    password: 
    database: 0

server:
  port: 8083

mybatis:
  mapper-locations: classpath:mapper/**/*.xml
  type-aliases-package: com.bone.masterdata.domain.model

logging:
  level:
    com.bone: debug
