
--- #################### 数据库相关配置 ####################
spring:
  datasource:
    url: jdbc:mysql://rds260kj5i2879nym9a9o.mysql.rds.aliyuncs.com:3306/ruoyi-vue-pro?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: yudao
    password: b16k_OZtM
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto
# Sa-Token 配置
sa-token:
  # Token名称
  token-name: satoken
  # 单个Token的长久有效期
  timeout: 2592000
  # Token的最低活跃频率
  active-timeout: 2592000
  # Token风格
  token-style: uuid

  sign:
    # API 接口调用签名秘钥
    secret-key: kQwIOrYvnXmSDkwEiFngrKidMcdrgKor

  sso-server:
    # Ticket有效期 (单位: 秒)，默认五分钟
    ticket-timeout: 300
    # 所有允许的授权回调地址
    # allow-url: http://sa-sso-client1.com/*,http://sa-sso-client2.com/*,http://127.0.0.1:*
    allow-url: "*"
  # 配置 Sa-Token 单独使用的 Redis 连接
  alone-redis:
    # Redis数据库索引（默认为0）
    database: 11
    # Redis服务器地址
    host: 47.105.112.106 # 地址
    # Redis服务器连接端口
    port: 6379
    # Redis服务器连接密码（默认为空）
    password: '@Pukang_123'
    # 连接超时时间
    timeout: 10s



forest:
  # 关闭 forest 请求日志打印
  log-enabled: false
