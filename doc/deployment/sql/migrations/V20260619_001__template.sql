-- ============================================================
-- 迁移脚本模板: 复制本文件并重命名为 V{YYYYMMDD}_{序号}__{描述}.sql
-- 日期: {填写日期}
-- 目的: {填写变更目的}
-- 影响范围: {填写影响的库/表}
-- 回滚方案: {填写回滚 SQL}
-- ============================================================

-- 在此编写迁移 SQL
-- 注意: 必须使用幂等语法（IF NOT EXISTS / IF EXISTS）
