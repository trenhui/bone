# 06 · AI 协作与编码准则

> `AGENTS.md` 拆分文档之一，承载原 **§11 给 AI 助手的关键提示**、**§12 AI 工程协作体系（V6.1）**。
> 拆分总索引见 [doc/agents/README.md](README.md)；规范入口见 [AGENTS.md](../../AGENTS.md)。
> **本文件是「AI 必须读」的那一份**：编码准则、自主权分级、交付流程、跨工具入口。

---

## 11. 给 AI 助手的关键提示

1. **不要破坏分层依赖**：修改代码时，`domain` 层不能引入 Spring/MyBatis 等框架依赖；`application` 层不能直接调用 `infrastructure` 实现类。
2. **保持 CQRS 与单一用例边界（v5.3.1）**：默认使用**语义化 `*ApplicationService`** 作为最外层应用用例边界（Application Service First + Selective CQRS，[ADR-0028](../architecture/adr/0028-application-service-first-selective-cqrs.md)）；写意图需显式契约/命令数量多时加 `Command`，命令需独立路由/异步/多入口时加 `*CommandHandler`。读侧：简单读（按 ID 详情/简单分页）由 ApplicationService 直查写 Repository；仅当读模型与聚合分歧（列表/搜索/统计/Join/组合投影）时在 ApplicationService 内引入 `QueryPort`，需独立路由/异步时才进 `QueryHandler → application QueryPort → infrastructure QueryAdapter`。`QueryBuilder` 不进入 application/domain。一个用例只选一种构件，禁止 Handler 与 ApplicationService 一对一套娃。Controller 禁止直注 `domain/service`、`domain/repository` 或 infrastructure；可注入合法 Handler、ApplicationService、Orchestrator，满足条件时可注入 Facade。现有 `application/usecase` / `*UseCase` 不新增，AI/Flow 能力发现使用 `com.bone.core.capability.@Capability`。详见 [应用用例边界](<../architecture/Bone-DDD-最终实践方案.md#application-use-case-boundary>)。
3. **统一响应格式**：Controller 返回统一使用 `ApiResponse<T>` 或 `PageResult<T>`，避免裸返回领域对象。
4. **租户与审计字段**：新增实体应继承 `TenantAbstractEntity`（若需多租户）或 `AbstractEntity`；不要遗漏 `tenantId` 与审计字段的填充。
5. **命名约定**（属于工程一致性，默认 Advisory；见 [DDD E-13](<../architecture/Bone-DDD-最终实践方案.md#naming-style>)）：
   - 聚合根：`{名词}`
   - 值对象：`{名词}`（如 `Username`）
   - 应用层命令：`{动作}{对象}Command`（如 `CreateUserCommand`）
   - 应用层查询：`{对象}{条件}Query`（如 `UserByIdQuery`）
   - 处理器：`*CommandHandler` / `*QueryHandler`
   - adapter 入参 DTO：`*Req` / `*Qry`（仅 adapter 层，与 application `*Query` 区分）
6. **前端微应用约束**：
   - 微应用使用 `vite-plugin-qiankun` 打包为 UMD，需配置 `fastRefresh: false`。
   - Shell 不直接使用 `vite-plugin-qiankun`，运行时通过 Qiankun JS API 加载微应用。
7. **格式化**：修改 Java 文件后，建议执行 `mvn spotless:apply` 保持格式一致。
8. **文档语言**：项目注释与文档以**中文**为主，新增代码注释请使用中文。
9. **Docs-as-Code（模块合规）**：平台模板见 [Docs-as-Code-模块合规模板](../architecture/Docs-as-Code-模块合规模板.md)。**扩展**：`tools/extension-compliance-collector` + 详设附录 A/C；**blueprint**：`tools/blueprint-compliance-collector` + `doc/_generated/blueprint/`。已落地能力**勿**写入各模块 `backlog.yaml`；PR 须提交对应 `_generated` 并通过 `collect.py --check`。
10. **持久化方案唯一性（强制）**：所有数据访问**必须**使用自研 `bone-metadata-sdk`（`@EnableSqlRepositories`），**禁止**引入 MyBatis-Plus、JPA / Hibernate、MyBatis 等其他 ORM 框架。新增模块的 `pom.xml` 不得添加上述框架依赖；仓储实现统一基于 `BaseRepository` / `SqlBuilder` 等 SDK 能力。
11. **简洁输出（ADHD 友好，全场景生效）**：回答直接给答案、可执行、少废话。
    - **先给答案**：首句直接给结论/下一步动作，不铺垫、不重复问题。
    - **编号步骤**：多步骤任务用编号列，每步一行、可执行；单列表 ≤5 项，超出分组或表格。
    - **以具体下一步收尾**：结尾给明确下一步动作，不说"如需帮助请告诉我"。
    - **时间估算**：用具体数字（"约 3 分钟"），不用"一会儿""很快"。
    - **砍客套**：不用"好的！""没问题！""希望有帮助！"等开场白/客套；指错不带情绪前缀，就事论事。
    - **进展可见**：长任务每轮开头 1 句重述当前进度，完成项打勾、剩余项列出。
12. **编码行为准则（先想后写 · 简单优先 · 精准修改 · 目标驱动）**：
    - **先想后写**：不默默假设。需求有歧义、或存在 ≥2 种合理方案时，**先确认再动手**，并呈现方案与取舍；禁止"猜一个就写"。
    - **简单优先**：用**能解决今天问题的最少代码**；不做超前设计，不加"将来可能用到"的抽象、配置项或扩展点。
    - **精准修改**：**只改必须改的**。不顺手重构 / 重命名 / 格式化任务范围外的代码，不顺带"优化"无关文件；发现范围外问题**只登记、不擅改**（另开任务）。
    - **目标驱动**：用**可验证的验收标准**代替模糊指令——
      > "Don't tell it what to do, give it success criteria and watch it go." —— Karpathy

      接到任务先明确"怎样算完成"（哪条命令通过、哪个测试变绿、什么输出算对），再动手；收尾时**逐条对照标准自证**，而非只说"已完成"。
    - **配套技能（CodeBuddy）**：触发判断与防违反（合理化借口表 / 红线清单）见技能 `ai-coding-discipline`（`.codebuddy/skills/ai-coding-discipline/SKILL.md`）。**§11.12 与 §11.13 为规范真源，该技能不重复条文**——规范变更一律改本文件，技能只同步触发条件与防违反部分。
13. **提交前自检清单（改动入库前逐项确认）**：
    - ① 分层依赖未破坏（`domain` 零框架依赖、`application` 不直连 `infrastructure`，§5.2）
    - ② 持久化仅用 `bone-metadata-sdk`（HC-001 / HC-006）
    - ③ 已执行 `mvn spotless:apply`，格式校验通过
    - ④ 受影响测试**已实际执行且通过**（非"应该能过"）
    - ⑤ 未改动任务范围外的文件
    - ⑥ 注释与文档同步更新，且为中文（§11.8）

---

## 12. AI 工程协作体系（V6.1）

> 本节定义 AI 编码工具的协作约束、执行流程和自主权边界。

### 12.1 硬约束清单（薄引用）

**HC-001～HC-008 的定义、判定方式与实测状态只有一处真源**：

> **[Bone-DDD-最终实践方案.md §G-1.7「HC 硬约束与实测状态」](<../architecture/Bone-DDD-最终实践方案.md#hc-hard-constraints>)**

本文件**不再并列维护 HC 表**。历史教训：本文件曾复制一份完整 HC 表，2026-09-17 实测 8 条中 **5 条与实现不符**（把无载体的条目写成"CI 阻断"、把覆盖率门槛 10% 写成 70%），并由此产生"以为有门禁、实际没有"的决策风险。规范 §G-3 第 7 条已把「门禁状态只有一处真源」列为准入项。

- 任何 HC 状态变更（含新增 HC-009+）**一律改 G-1.7**，本文件不再同步副本。
- 引用 HC 时写 `HC-00x` 编号即可，**不要在此处复述状态**。
- 「Planned / Manual」不得在任何评审、工单、对外材料中写成 `Active`。

### 12.2 动态上下文加载路由

AI 执行任务前，按修改路径加载对应上下文。**本节是 `doc/agents/` 拆分文档的加载路由**（`AGENTS.md` 只保留同一张表的摘要）：

| 修改路径 | 必须加载的上下文 |
|---------|----------------|
| `**/domain/**` | [03 架构分层规范](03-架构分层规范.md) + `doc/architecture/Bone-DDD-最终实践方案.md` + `doc/design/modules/` 对应模块详设 |
| `**/application/**` | [03 架构分层规范](03-架构分层规范.md) + `doc/architecture/Bone-DDD-最终实践方案.md`（应用用例、CQRS、事务与一致性章节） |
| `**/adapter/web/**` | `doc/architecture/Bone-API-规范.md` + `doc/architecture/openapi/` 对应 YAML |
| `**/infrastructure/**` | [05 数据库与安全](05-数据库与安全.md) + `doc/architecture/数据库开发规范.md` + `bone-engine/bone-metadata-sdk/README.md` |
| `bone-frontend/**` | `doc/architecture/bone-前端架构.md` §6（API错误处理/状态管理/微前端通信） |
| 端口 / 模块结构 / 构建命令 | [01 项目概览与模块结构](01-项目概览与模块结构.md) + [02 构建·运行·部署](02-构建运行与部署.md) |
| 测试 / 质量门禁 | [04 测试与代码质量](04-测试与代码质量.md) |
| 任何文件 | 本文件（§11 编码准则 + §12 协作体系） |

### 12.3 AI 自主权分级

| 等级 | 范围 | 审查要求 |
|------|------|---------|
| **L0** | 格式化、注释、局部变量重命名、添加 import | 无需审查 |
| **L1** | 编写单元测试、生成 DTO/Req/Resp、补充 OpenAPI | 必须 `./scripts/check.sh` 通过 |
| **L2** | 新增 Controller/Handler 方法、修改业务逻辑 | 需 PR 双人 Review |
| **L3** | 修改 DDL、删除已有代码、升级依赖版本、修改 CI 脚本 | 必须架构师审批 |
| **L4** | 生产数据库迁移、密钥/证书管理、发布打 tag、修改本节内容 | **完全禁止 AI 执行** |

> **拆分后的 L4 边界**：L4 覆盖**本节全部内容（§12）**，即 `doc/agents/06-AI协作与编码准则.md` 的 §12 部分与 `AGENTS.md` 的协作条款。AI 不得自行修改这些部分——需架构师本人执行或明确授权。

### 12.4 熔断机制（机器强制）

- Pre-commit hook 记录 `scripts/check.sh` 连续失败次数
- 第 3 次失败后，hook **物理拒绝执行**，输出：`[MELTDOWN] 架构约束连续失败 3 次，需人工介入。`
- 清除计数：`rm .git/hooks/.check-fail-count`
- 这是**机器级熔断**，不依赖 AI 自觉遵守

### 12.5 标准交付流程

```
P1 需求理解 → 确认所属模块和 Bounded Context + **定义可验证验收标准**（§11.12 目标驱动）
    ↓
P2 上下文加载 → 按 §12.2 路由表加载文档
    ↓
P3 契约对齐 → 修改/确认 doc/architecture/openapi/ 设计态 YAML
    ↓ （人工审批：契约变更）
P4 代码实现 → 按 DDD 分层 + bone-metadata-sdk 实现
    ↓
P5 本地自检 → ./scripts/check.sh（≤3 次重试，超过则熔断）
    ↓
P6 提交 PR → CI 全量门禁（ArchUnit + JaCoCo + oasdiff；Gitleaks 目前未接入 CI，见 G-1.7）
    ↓
P7 人工 Review → 双人审查 + 架构师审批（L3 变更）
    ↓
P8 合并 → 触发 Docs-as-Code 合规收集器更新 _generated/
```

### 12.6 跨工具入口适配

各 AI 工具的入口文件均薄引用规范入口：

| 工具 | 入口文件 | 内容 |
|------|---------|------|
| 通用 | `AGENTS.md` | 薄引用入口（唯一真源 + 加载路由） |
| 本手册 | `doc/agents/*.md` | 拆分后的完整说明（本文是其中之一） |
| Claude Code | `CLAUDE.md` | `@AGENTS.md`（薄引用） |
| Cursor | `.cursorrules` | `Read AGENTS.md first.` |
| Copilot | `.github/copilot-instructions.md` | `See AGENTS.md.` |

> **禁止用外部通用规范直接覆盖入口文件**：任何"下载第三方 CLAUDE.md 覆盖本地入口"、或把通用 AI 编码准则整体粘贴进
> `CLAUDE.md` / `.cursorrules` / `copilot-instructions.md` 的做法，都会破坏薄引用架构，并**丢失项目特有约束**
> （DDD 分层门禁、bone-metadata-sdk 唯一持久化、多租户与软删等）。外部优秀准则一律**先吸收进本手册**，入口文件保持薄引用。
>
> **同理禁止把拆分文档的内容抄回入口文件**：`AGENTS.md` 只放摘要与路由；重复一份就会立刻漂移（§12.1 就是前例）。

### 12.7 与已有资产的关系

| 已有资产 | 定位 | 与本节关系 |
|---------|------|-----------|
| `.claude/agents/` | Claude Code 专用 agent 定义 | 引用本节约束，不重复定义 |
| `.claude/contracts/` | L1/L2 功能契约模板 | 修正 `mapper` → `repository`，引用 HC-001~008 |
| `.claude/hooks/` | Claude Code 专用钩子 | 保留，与 `scripts/check.sh` 互补 |
| `tools/*-compliance-collector/` | Docs-as-Code CI 派生 | 保留，在 P8 阶段执行 |
| `doc/architecture/adr/` | 架构决策记录 | 约束的决策依据，HC 条目引用 ADR 编号 |
| 【修正】ArchUnit `*ArchitectureTest` | 8 个模块的架构测试 | **仅** HC-002 的执行载体；HC-003 与 HC-006 **无共享规则、无机械载体**（G-1.7 标 Planned），不得宣称已由 CI 阻断 |
| 【修正】`scripts/scan-secrets.sh` / `scripts/check.sh` | 本地预提交与安全检查 | HC-001（ORM 扫描）、HC-004（密钥扫描）的**本地**载体；**无工作流调用**，不是 CI 门禁 |
| 【修正】`scripts/check-ddl-doc-sync.py` | 表名清单比对 | 只比对表名，**不读列**；HC-008 因此标 Planned |

---

**上级入口**：[AGENTS.md](../../AGENTS.md) ｜ **拆分总索引**：[doc/agents/README.md](README.md)
**同系列**：[01 项目概览与模块结构](01-项目概览与模块结构.md) ｜ [02 构建·运行·部署](02-构建运行与部署.md) ｜ [03 架构分层规范](03-架构分层规范.md) ｜ [04 测试与代码质量](04-测试与代码质量.md) ｜ [05 数据库与安全](05-数据库与安全.md)
