# Bone 数据库开发规范

> **状态**：已采纳（2026-05-16，方案 C：时间字段统一 `*_at`）  
> **设计对齐**：[`Bone-DDD-最终实践方案.md`](./Bone-DDD-最终实践方案.md) · `bone-core` `AbstractEntity`  
> **目标数据库**：MySQL 8.0+ · 库名 `bone` · InnoDB · `utf8mb4` / `utf8mb4_unicode_ci`

---

## 1. 总则与真源

| 项 | 规范 |
|----|------|
| **唯一可执行 DDL** | 仓库根目录 [`bone-init.sql`](../../bone-init.sql) |
| **变更方式** | 修改 `bone-init.sql` → 开发库 `DROP DATABASE bone` 后全量重建 |
| **主键** | `id BIGINT NOT NULL`，应用层 Snowflake（**禁止** `AUTO_INCREMENT`） |
| **租户** | 业务表含 `tenant_id BIGINT NOT NULL DEFAULT 0`（0 = 平台）；`biz_identity_code` **默认仅在 JWT/上下文**，不强制落库，需要时按模块 ADR 补列（见 [Bone-多租户规范 §1](./Bone-多租户规范.md)） |
| **软删** | `deleted TINYINT(1) NOT NULL DEFAULT 0` |
| **乐观锁** | 聚合根表含 `version INT NOT NULL DEFAULT 0`（纯关联表、只追加日志表可省略） |
| **操作人** | `created_by`、`updated_by`（`BIGINT`，可 NULL） |
| **时间** | **全部** `DATETIME(3)`，列名统一 **`{语义}_at`**（见 §4） |
| **表命名** | `{模块前缀}_{业务名}`，全小写蛇形；表/列/索引须有中文 COMMENT |
| **外键** | 不建物理外键；参照完整性由应用层保证；关联列建索引 |

**禁止**：Flyway 增量迁移、双轨 DDL、`biz_id` 双 ID、`iam_user` / `int_flow_execution` / `integ_` 前缀、**`create_time` / `update_time` / `create_by` / `update_by`**（历史命名）。

| 产物 | 说明 |
|------|------|
| [`bone-init.sql`](../../bone-init.sql) | 唯一真源（当前 **53** 张表；含 IAM 9 + System 4 + Integration 8 + Metadata 5 + Extension 5 + Generator 7 + Console 3 + Masterdata 8 + 兼容 2 + 其他 2） |
| ~~[`初始脚本.sql`](./初始脚本.sql)~~、~~[`DDL对齐说明.md`](./DDL对齐说明.md)~~ | 已废止 |

各服务：`spring.flyway.enabled: false`，`spring.sql.init.mode: never`。

---

## 2. `bone-init.sql` 表清单（当前）

### 2.1 IAM（12）

`iam_tenant`、`iam_account`、`iam_role`、`iam_permission`、`iam_account_role`、`iam_role_permission`、`iam_audit_log`、`iam_policy`、`iam_refresh_token`、`iam_audit_settings`、`iam_dept`、`iam_menu`

### 2.2 System（6）

`sys_config`、`sys_log`、`sys_alert_rule`、`sys_alert_event`、`sys_dict`、`sys_schedule_task`

### 2.3 Integration（8）

`int_connector`、`int_flow`、`int_flow_node`、`int_flow_connection`、`int_execution_log`、`int_dead_letter`、`int_outbox`、`int_template`

> 执行日志表名为 **`int_execution_log`**（非 `int_flow_execution`）。`int_outbox` 用于事务消息发件箱（领域事件 → MQ 一致投递），见 [Bone-消息与事件规范](./Bone-消息与事件规范.md)。

### 2.4 Metadata（7）

`meta_entity`（含 `delivery_mode`：0-GENERATIVE / 1-RUNTIME）、`meta_field`、`meta_entity_relation`、`meta_code_template`、`meta_data_quality_rule`、`meta_data_lineage`、`meta_data_standard`

### 2.5 Extension Studio（5）

`exts_extension_point`、`exts_extension_impl`、`exts_plugin_version`、`exts_plugin_execution_log`、`exts_audit_log`

> **遗留表名迁移**：若本地库仍存在旧前缀 `xst_*` 或 `ext_studio_*`，请 `DROP DATABASE bone` 后重新执行 `bone-init.sql`（见 ADR-数据库迁移与DDL真源）。

> **遗留 `codegen_*`**：`studio-generator` 模块私有表，**不**在 `bone-init` 中（见模块内测试资源或 Flyway 脚本）。

### 2.6 Studio Generator（7）

`gen_data_source`、`gen_table_metadata`、`gen_column_metadata`、`gen_code_template`、`gen_type_mapping`、`gen_generation_task`、`gen_code_generation_history`

### 2.7 统一控制台（3）

`cnsl_dashboard_widget`、`cnsl_notification`、`cnsl_recent_access`

### 2.8 主数据（9 + 2 兼容）

`mdm_entity`、`mdm_record`、`mdm_record_version`、`mdm_category`、`mdm_record_category`、`mdm_qcheck_task`、`mdm_qcheck_detail`、`mdm_qcheck_report`、`md_field`

运行时兼容（`bone-masterdata` 领域模型过渡，计划收敛至 `mdm_*`）：`md_entity`、`md_record`

### 2.9 应用与模块（2）

`bone_application`、`bone_module`

### 2.10 通知（1）

`ntf_message`

### 2.11 蓝图样板（Blueprint，5）

`bp_payment`、`bp_outbox`、`bp_processed_event`、`t_order`、`t_order_item`

> **样板表命名**：支付 / Outbox / 幂等表使用蓝图模块前缀（bp_ 系列）；订单上下文沿用 `t_order` / `t_order_item`（示例命名，读者对照通用订单模型更直观），**不**作为其他模块命名先例——新模块一律用 §3 的模块前缀。
>
> **`t_order_item` 不落 `tenant_id`**：它是 `t_order` 聚合内实体，租户隔离经父聚合 `tenant_id` 间接保证（子表例外，已在两处 DDL 与模块 README 说明）。HC-008 的「新表含 `tenant_id`」对这些**纯子表**按此例外处理；独立聚合表仍强制。
>
> **蓝图表曾长期只存在于模块 `schema.sql`**（双轨 DDL，2026-09-16 收敛）：现已全部并入本文真源，模块侧删除建表脚本并置 `spring.sql.init.mode: never`。

---

## 3. 模块前缀

| 前缀 | 模块 | 在 init 中 |
|------|------|------------|
| `iam_` | IAM | ✅ |
| `sys_` | 系统管理 | ✅ |
| `int_` | 集成 | ✅ |
| `meta_` | 元数据 | ✅ |
| `exts_` | 扩展 Studio（**EXT**ension**S** 控制面；单段前缀，与 metadata `ext_data_*` 区分） | ✅ |
| `gen_` | Studio Generator（`studio-generator`） | ✅ |
| `codegen_` | 遗留工具 `bone-codegen`（**非** `bone-init` 真源） | — |
| `mdm_` | 主数据 | ✅ |
| `cnsl_` | 统一控制台 | ✅ |
| `bp_` | 蓝图样板（`bone-blueprint`） | ✅ |
| `arch_` / `evt_` | 待落地 | — |

---

## 4. 字段命名与类型

### 4.1 时间字段（方案 C：统一 `*_at`）

**所有时间点列**（含行创建/修改、业务事件）均使用 **`{动词或语义}_at`**，类型 **`DATETIME(3)`**。

| 用途 | 列名 | 说明 |
|------|------|------|
| 行创建时刻 | `created_at` | 默认 `CURRENT_TIMESTAMP(3)` |
| 行最后修改时刻 | `updated_at` | 默认 `ON UPDATE CURRENT_TIMESTAMP(3)` |
| 业务事件 | `last_login_at`、`expires_at`、`started_at`、`published_at`… | 由领域显式赋值，通常允许 NULL 直至事件发生 |

**行级四件套（推荐，聚合根表）**：`created_by`、`updated_by`、`created_at`、`updated_at`。

**例外（须在 COMMENT 说明）**：

| 场景 | 处理 |
|------|------|
| 只追加、永不 UPDATE 的表（如 `iam_audit_log`） | 仅 `created_at`，无 `updated_at` |
| 令牌/会话 | `expires_at`、`revoked_at` 等业务列 + 可选 `created_at` |

**禁止**：`create_time` / `update_time`、`created_at` 与 `row_created_at` 并存、`TIMESTAMP` 无时区策略的混用。

**Java 映射**：`createdAt` / `updatedAt`（`LocalDateTime` 或 `Date`），与列名蛇形转换一致；API JSON 建议 ISO-8601。

### 4.2 其它类型

- **布尔**：`is_xxx TINYINT(1)`；枚举态用 `status` + COMMENT。
- **关联列**：`{实体}_id`。
- **金额**：`DECIMAL(18,2)`（报表类可用 `DECIMAL(20,4)`，模块内统一）。
- **配置**：`JSON`（结构由契约/模块定义）。

### 4.3 布尔与 JDBC

- `TINYINT(1)` ↔ `boolean` / `Boolean`。
- 三态用包装类型 + 列 `NULL`。

---

## 5. 索引规范

| 类型 | 命名 | 示例 |
|------|------|------|
| 唯一索引 | `uk_{表缩写}_{字段}` | `uk_iam_tenant_code` |
| 普通索引 | `idx_{表缩写}_{字段}` | `idx_iam_account_tenant` |

- 多租户表组合索引：**`tenant_id` 置前**，如 `idx_xxx_tenant_status (tenant_id, status)`。
- 组合索引：区分度高的列在前；等值在前、范围在后。
- **不对 `deleted` 单独建索引**；按查询模式并入组合索引。
- 软删 + 业务唯一：采用 `(tenant_id, code)` 唯一 + 应用层保证已删行释放编码，或唯一索引包含 `deleted`（团队择一并在模块注明）。

---

## 6. 时区与默认值（业界实践）

| 项 | 建议 |
|----|------|
| **库内存储** | 统一 **UTC**（推荐）或与运维约定一致的单一时区；禁止混用 |
| **应用层** | `Instant` / `OffsetDateTime` 转换后写入；展示层按用户时区格式化 |
| **默认值** | `created_at` / `updated_at` 可用 DB 默认；**业务 `xxx_at`** 默认 `NULL`，事件发生时写入 |
| **API** | 对外 ISO-8601（含偏移或 `Z`） |

---

## 7. 分区与大表

- 日志/流水类可按 **`created_at` 或业务 `started_at`** 做 RANGE 分区（年/月）。
- 分区键须包含在**主键与所有唯一索引**中。
- 约定保留周期与归档脚本（运维）。

---

## 8. 安全与敏感数据

- 密码、密钥、Token：**哈希或 KMS**，列如 `password_hash`、`token_hash`；禁止明文落库。
- `encrypted` / 敏感配置：生产通过环境变量或配置中心注入，禁止写死在仓库。
- 日志表不得写入完整密钥、身份证号等（见日志规范）。

---

## 9. 变更与评审

1. 修改 [`bone-init.sql`](../../bone-init.sql)。
2. 同步 **Java 领域模型**与 Metadata SDK SQL（列名 `snake_case` ↔ 字段 `camelCase`）。
3. 更新本文 **§2 表清单**。
4. Code Review 后重建开发库：

```bash
mysql -u root -p -e "DROP DATABASE IF EXISTS bone;"
mysql -u root -p < bone-init.sql
```

---

## 10. 修订记录

| 日期 | 说明 |
|------|------|
| 2026-05-16 | **方案 C**：时间/操作人统一 `created_at`、`updated_at`、`created_by`、`updated_by` 及业务 `xxx_at` |
| 2026-05-16 | 单轨 `bone-init.sql`；合并原 DDL 最终规范 |
| 2026-05-17 | Studio 表前缀定为 `exts_`（可读性优于 `xst_`） |
| 2026-05-17 | `exts_*` 补充 `tenant_id` 领先组合索引；Studio `metadata-mysql` profile |
| 2026-05-17 | 追加 `exts_*` 四表（29 张表）；时间字段方案 C |
