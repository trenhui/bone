openapi: 3.1.0
info:
  title: Bone Extension Studio API
  version: 1.2.0
  description: |
    扩展引擎管理台（As-Is）。规范见 Bone-API-规范.md §13.1；
    详设 v2.1：doc/design/modules/5. 扩展管理模块详细设计方案.md。

    **术语**：路径 `/plugins/{id}` 的 id = 扩展实现 ID（exts_extension_impl），非独立插件主表。

    **[Target]**：POST 创建 201+Location；写操作 Idempotency-Key；PUT If-Match/version；
    大制品 :deploy 支持 202 LRO（默认异步）与 ?sync=true 同步 200。
servers:
  - url: /api/v1/extension
paths:
  /points:
    get:
      summary: 扩展点列表
      parameters:
        - { name: keyword, in: query, schema: { type: string } }
        - { name: domain, in: query, schema: { type: string } }
        - { name: category, in: query, schema: { type: string } }
        - { name: page, in: query, schema: { type: integer } }
        - { name: size, in: query, schema: { type: integer } }
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                $ref: './components/ApiResponse.yaml'
    post:
      summary: 创建扩展点
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '201':
          description: Created（[Target] Location 头）
        '200':
          description: OK（As-Is 兼容）
  /points/{id}:
    get:
      summary: 扩展点详情
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
      responses:
        '200': { description: OK }
    put:
      summary: 更新扩展点
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
        - $ref: '#/components/parameters/IfMatch'
      responses:
        '200': { description: OK }
        '412': { description: Precondition Failed（[Target] 乐观锁） }
    delete:
      summary: 删除扩展点
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
      responses:
        '204': { description: No Content }
  /points/{id}:enable:
    post:
      summary: 启用扩展点
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
      responses:
        '200': { description: OK }
  /points/{id}:disable:
    post:
      summary: 禁用扩展点
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
      responses:
        '200': { description: OK }
  /plugins:
    get:
      summary: 扩展实现列表（控制台称插件）
      responses:
        '200': { description: OK }
    post:
      summary: 创建扩展实现元数据
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '201': { description: Created（[Target]） }
        '200': { description: OK（As-Is） }
  /plugins/{id}:
    get:
      summary: 扩展实现详情
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
      responses:
        '200': { description: OK }
    put:
      summary: 更新扩展实现
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
        - $ref: '#/components/parameters/IfMatch'
      responses:
        '200': { description: OK }
    delete:
      summary: 删除扩展实现
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
      responses:
        '204': { description: No Content }
  /plugins:upload:
    post:
      summary: 上传 JAR 制品
      parameters:
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '201': { description: Created }
        '200': { description: OK（As-Is） }
  /plugins/{id}:deploy:
    post:
      summary: 部署/激活扩展实现
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
        - { name: sync, in: query, schema: { type: boolean }, description: true 强制同步 200；false 强制 202 LRO }
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200': { description: OK（同步，?sync=true 或配置 deploy-sync-by-default） }
        '202': { description: Accepted（LRO，Location 指向 /operations/{id}） }
  /operations/{operationId}:
    get:
      summary: LRO 操作状态轮询
      parameters:
        - { name: operationId, in: path, required: true, schema: { type: string } }
      responses:
        '200':
          description: 含 done、progress、result、error（ApiResponse 信封）
        '404': { description: 操作不存在 }
  /plugins/{id}:undeploy:
    post:
      summary: 卸载
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
      responses:
        '200': { description: OK }
  /plugins/{id}:rollback:
    post:
      summary: 回滚制品版本
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
        - $ref: '#/components/parameters/IdempotencyKey'
      responses:
        '200': { description: OK }
  /plugins/{id}:publish-runtime:
    post:
      summary: 发布运行时路由元数据
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
      responses:
        '200': { description: OK }
  /plugins/{id}:simulate:
    post:
      summary: 沙箱模拟执行
      parameters:
        - { name: id, in: path, required: true, schema: { type: integer, format: int64 } }
      responses:
        '200': { description: OK }
  /execution-logs:
    get:
      summary: 执行日志（默认游标分页）
      parameters:
        - { name: pluginId, in: query, schema: { type: integer, format: int64 } }
        - { name: status, in: query, schema: { type: string } }
        - { name: cursor, in: query, schema: { type: string } }
        - { name: limit, in: query, schema: { type: integer, default: 20 } }
        - { name: page, in: query, description: 遗留 offset 分页, schema: { type: integer } }
        - { name: size, in: query, schema: { type: integer } }
      responses:
        '200':
          description: OK
          headers:
            Link:
              schema: { type: string }
              description: 'rel="next" 游标链接'
  /execution-logs:ingest:
    post:
      summary: SDK 执行日志上报
      responses:
        '200': { description: OK }
  /overview:
    get:
      summary: 扩展概览
      responses:
        '200': { description: OK }
  /sandbox/config:
    get:
      summary: 沙箱配置
      responses:
        '200': { description: OK }
  /audit-logs:
    get:
      summary: Studio 审计日志（游标分页）
      parameters:
        - { name: action, in: query, schema: { type: string } }
        - { name: resourceType, in: query, schema: { type: string } }
        - { name: cursor, in: query, schema: { type: string } }
        - { name: limit, in: query, schema: { type: integer, default: 20 } }
      responses:
        '200':
          description: OK
          headers:
            Link:
              schema: { type: string }
components:
  parameters:
    IdempotencyKey:
      name: Idempotency-Key
      in: header
      required: false
      description: '[Target] 幂等写；TTL 24h，见 Bone-API-规范 §8'
      schema: { type: string, format: uuid }
    IfMatch:
      name: If-Match
      in: header
      required: false
      description: '[Target] 乐观锁，值为实体 version 或 ETag'
      schema: { type: string }
