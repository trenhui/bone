# 🏗️ Bone 企业级开发平台：Java 工程实践规范

## 📋 目录
1. [架构设计原则](#架构设计原则)
2. [项目结构规范](#项目结构规范)
3. [包命名规范](#包命名规范)
4. [类命名规范](#类命名规范)
5. [代码风格规范](#代码风格规范)
6. [Maven配置规范](#maven配置规范)
7. [数据库设计规范](#数据库设计规范)
8. [API设计规范](#api设计规范)

---

## 🎯 架构设计原则

### 1.1 核心设计理念
```java
/**
 * Bone 平台架构核心原则
 * 1. 分层架构：严格分层，单向依赖
 * 2. 模块化：高内聚，低耦合
 * 3. 元数据驱动：配置即代码
 * 4. 扩展优先：SPI + 插件化
 * 5. 企业级就绪：多租户、安全、监控
 */
```

### 1.2 架构层次依赖
```
bone-core → bone-engine → bone-platform → bone-services
    ↑                                      ↑
    └────────── bone-sdk ──────────────────┘
    ↑
bone-tools
```

### 1.3 技术选型标准
- **Java 17+**: LTS版本，现代语言特性
- **Spring Boot 3.2+**: 企业级开发框架
- **Spring Cloud**: 微服务治理
- **MyBatis Plus**: 数据访问层
- **Caffeine/Redis**: 多级缓存
- **HikariCP**: 数据库连接池

---

## 📁 项目结构规范

### 2.1 标准项目结构
```bash
bone/
├── 📦 bone-parent/                    # 父工程
├── 🔧 bone-core/                      # 核心原子层
├── ⚙️ bone-engine/                    # 引擎驱动层
├── 🏢 bone-platform/                  # 平台服务层
├── 🚀 bone-services/                  # 业务服务层
├── 📦 bone-starters/                  # Starter集成层
├── 🔌 bone-sdk/                       # 客户端SDK
├── 🛠️ bone-tools/                     # 开发工具
└── 💡 bone-examples/                  # 示例项目
```

### 2.2 模块职责定义

| 层级 | 模块 | 职责 | 依赖关系 |
|------|------|------|----------|
| **核心层** | bone-core | 基础工具、安全、数据源 | 无业务依赖 |
| **引擎层** | bone-engine | 元数据、扩展、工作流引擎 | 依赖core |
| **平台层** | bone-platform | 认证、账户、主数据服务 | 依赖engine |
| **服务层** | bone-services | 具体业务服务实现 | 依赖platform |
| **集成层** | bone-starters | Spring Boot自动配置 | 依赖各层 |
| **客户端** | bone-sdk | 多端SDK集成 | 依赖core/engine |

---

## 📦 包命名规范

### 3.1 根包命名
```java
// 标准根包名
com.bone.{layer}.{module}.{component}

// 示例
com.bone.core.common.util
com.bone.engine.metadata.service
com.bone.platform.auth.oauth2
com.bone.services.admin.system
```

### 3.2 分层包结构
```java
// 核心层
com.bone.core
├── common/          # 通用组件
├── security/        # 安全框架  
├── datasource/      # 数据访问
├── cache/           # 缓存抽象
└── observability/   # 可观测性

// 引擎层
com.bone.engine
├── metadata/        # 元数据引擎
├── extension/       # 扩展引擎
├── integration/     # 集成引擎
└── workflow/        # 工作流引擎

// 平台层  
com.bone.platform
├── auth/           # 认证中心
├── account/        # 账户中心
└── masterdata/     # 主数据
```

### 3.3 组件包命名
```java
// 标准组件包
.model/              # 数据模型
.entity/             # 实体类
.dto/                # 数据传输对象
.vo/                 # 视图对象
.service/            # 服务接口
.service.impl/       # 服务实现
.controller/         # 控制器
.repository/         # 数据访问
.config/             # 配置类
.util/               # 工具类
.exception/          # 异常类
.handler/            # 处理器
.interceptor/        # 拦截器
.aspect/             # 切面
```

---

## 🏷️ 类命名规范

### 4.1 类命名标准

| 类型 | 命名模式 | 示例 | 说明 |
|------|----------|------|------|
| 实体类 | XxxEntity | `UserEntity` | 数据库实体 |
| DTO类 | XxxDTO | `UserDTO` | 数据传输对象 |
| VO类 | XxxVO | `UserVO` | 视图对象 |
| 服务接口 | XxxService | `UserService` | 服务接口 |
| 服务实现 | XxxServiceImpl | `UserServiceImpl` | 服务实现类 |
| 控制器 | XxxController | `UserController` | REST控制器 |
| 仓库接口 | XxxRepository | `UserRepository` | 数据访问接口 |
| 配置类 | XxxConfig | `DataSourceConfig` | 配置类 |
| 工具类 | XxxUtil | `DateUtil` | 工具类 |
| 异常类 | XxxException | `BusinessException` | 异常类 |
| 枚举类 | XxxEnum | `StatusEnum` | 枚举类 |
| 常量类 | XxxConstants | `SystemConstants` | 常量类 |
| 事件类 | XxxEvent | `UserCreatedEvent` | 领域事件 |
| 处理器 | XxxHandler | `GlobalExceptionHandler` | 事件处理器 |

### 4.2 注解命名规范
```java
// 元数据注解
@SObject(name = "User", label = "用户")
@StandardField(name = "username", type = FieldType.STRING)
@CustomField(name = "industry", storage = StorageType.RESERVED)
@ReservedColumn(index = 1)

// Spring注解
@RestController
@Service
@Repository
@Configuration
@Component
```

---

## ✍️ 代码风格规范

### 5.1 方法命名规范

| 操作类型 | 命名模式 | 示例 | 说明 |
|----------|----------|------|------|
| 查询单个 | `getXxxById` | `getUserById(Long id)` | 根据ID查询 |
| 条件查询 | `findXxxByYyy` | `findUserByUsername(String name)` | 条件查询 |
| 查询列表 | `listXxxs` | `listUsers()` | 查询列表 |
| 分页查询 | `pageXxxs` | `pageUsers(PageQuery query)` | 分页查询 |
| 统计数量 | `countXxx` | `countUsers()` | 统计数量 |
| 创建对象 | `createXxx` | `createUser(UserDTO user)` | 创建操作 |
| 更新对象 | `updateXxx` | `updateUser(UserDTO user)` | 更新操作 |
| 删除对象 | `deleteXxx` | `deleteUser(Long id)` | 删除操作 |
| 保存对象 | `saveXxx` | `saveUser(UserDTO user)` | 保存操作 |
| 验证对象 | `validateXxx` | `validateUser(UserDTO user)` | 数据验证 |
| 处理业务 | `processXxx` | `processOrder(OrderDTO order)` | 业务处理 |
| 执行操作 | `executeXxx` | `executeTask(TaskDTO task)` | 执行操作 |

### 5.2 变量命名规范
```java
// 类成员变量 - 小驼峰
private String userName;
private LocalDateTime createTime;
private List<User> userList;

// 静态常量 - 全大写+下划线
public static final int MAX_RETRY_COUNT = 3;
public static final String DEFAULT_CHARSET = "UTF-8";
public static final String SYSTEM_ADMIN = "admin";

// 方法参数 - 小驼峰
public User getUserById(Long userId, String userName)

// 局部变量 - 小驼峰
List<User> activeUsers = userService.listActiveUsers();
int totalCount = userRepository.countUsers();
```

### 5.3 代码格式规范
```java
/**
 * 用户服务实现类
 */
@Service
@Slf4j
@RequiredArgsConstructor
public class UserServiceImpl implements UserService {
    
    private final UserRepository userRepository;
    private final PasswordEncoder passwordEncoder;
    
    /**
     * 创建用户
     */
    @Override
    @Transactional(rollbackFor = Exception.class)
    public UserVO createUser(UserCreateDTO createDTO) {
        // 参数验证
        ValidateUtil.validate(createDTO);
        
        // 业务逻辑
        if (userRepository.existsByUsername(createDTO.getUsername())) {
            throw new BusinessException("用户名已存在");
        }
        
        // 数据转换
        UserEntity userEntity = UserConverter.INSTANCE.toEntity(createDTO);
        userEntity.setPassword(passwordEncoder.encode(createDTO.getPassword()));
        
        // 保存数据
        UserEntity savedEntity = userRepository.save(userEntity);
        
        // 返回结果
        return UserConverter.INSTANCE.toVO(savedEntity);
    }
    
    /**
     * 分页查询用户
     */
    @Override
    public PageResult<UserVO> pageUsers(UserQueryDTO queryDTO) {
        // 构建查询条件
        LambdaQueryWrapper<UserEntity> queryWrapper = new LambdaQueryWrapper<>();
        queryWrapper.eq(StringUtils.isNotBlank(queryDTO.getUsername()), 
                       UserEntity::getUsername, queryDTO.getUsername())
                   .eq(queryDTO.getStatus() != null, 
                       UserEntity::getStatus, queryDTO.getStatus())
                   .orderByDesc(UserEntity::getCreateTime);
        
        // 执行分页查询
        Page<UserEntity> page = userRepository.selectPage(
            new Page<>(queryDTO.getPageNum(), queryDTO.getPageSize()), 
            queryWrapper
        );
        
        // 转换结果
        List<UserVO> userVOs = UserConverter.INSTANCE.toVOList(page.getRecords());
        return new PageResult<>(userVOs, page.getTotal(), page.getSize(), page.getCurrent());
    }
}
```

---

## 🛠️ Maven配置规范

### 6.1 父POM配置
```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.bone</groupId>
    <artifactId>bone-parent</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>Bone Platform Parent</name>
    <description>Bone Enterprise Development Platform</description>

    <modules>
        <module>bone-core</module>
        <module>bone-engine</module>
        <module>bone-platform</module>
        <module>bone-services</module>
        <module>bone-starters</module>
        <module>bone-sdk</module>
        <module>bone-tools</module>
        <module>bone-examples</module>
    </modules>

    <properties>
        <!-- 基础配置 -->
        <java.version>17</java.version>
        <maven.compiler.source>${java.version}</maven.compiler.source>
        <maven.compiler.target>${java.version}</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.version>3.11.0</maven.compiler.version>
        
        <!-- Bone版本 -->
        <bone.version>1.0.0-SNAPSHOT</bone.version>
        
        <!-- 第三方依赖版本 -->
        <spring-boot.version>3.2.0</spring-boot.version>
        <spring-cloud.version>2023.0.0</spring-cloud.version>
        <mybatis-plus.version>3.5.5</mybatis-plus.version>
        <lombok.version>1.18.30</lombok.version>
        <jackson.version>2.15.0</jackson.version>
        <caffeine.version>3.1.8</caffeine.version>
    </properties>

    <!-- 依赖管理 -->
    <dependencyManagement>
        <dependencies>
            <!-- Spring Boot BOM -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            
            <!-- Bone内部模块统一管理 -->
            <dependency>
                <groupId>com.bone</groupId>
                <artifactId>bone-common</artifactId>
                <version>${bone.version}</version>
            </dependency>
            <dependency>
                <groupId>com.bone</groupId>
                <artifactId>bone-datasource</artifactId>
                <version>${bone.version}</version>
            </dependency>
            <dependency>
                <groupId>com.bone</groupId>
                <artifactId>bone-metadata</artifactId>
                <version>${bone.version}</version>
            </dependency>
            <dependency>
                <groupId>com.bone</groupId>
                <artifactId>bone-auth</artifactId>
                <version>${bone.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <!-- 构建配置 -->
    <build>
        <pluginManagement>
            <plugins>
                <!-- 编译器插件 -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-compiler-plugin</artifactId>
                    <version>${maven.compiler.version}</version>
                    <configuration>
                        <source>${java.version}</source>
                        <target>${java.version}</target>
                        <encoding>${project.build.sourceEncoding}</encoding>
                        <parameters>true</parameters>
                    </configuration>
                </plugin>
                
                <!-- Spring Boot插件 -->
                <plugin>
                    <groupId>org.springframework.boot</groupId>
                    <artifactId>spring-boot-maven-plugin</artifactId>
                    <version>${spring-boot.version}</version>
                    <configuration>
                        <excludes>
                            <exclude>
                                <groupId>org.projectlombok</groupId>
                                <artifactId>lombok</artifactId>
                            </exclude>
                        </excludes>
                    </configuration>
                </plugin>
                
                <!-- 代码质量检查 -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-checkstyle-plugin</artifactId>
                    <version>3.2.0</version>
                    <configuration>
                        <configLocation>checkstyle.xml</configLocation>
                        <encoding>UTF-8</encoding>
                        <consoleOutput>true</consoleOutput>
                        <failsOnError>true</failsOnError>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
        
        <!-- 资源过滤 -->
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>true</filtering>
                <includes>
                    <include>**/*.properties</include>
                    <include>**/*.yml</include>
                    <include>**/*.yaml</include>
                </includes>
            </resource>
            <resource>
                <directory>src/main/resources</directory>
                <filtering>false</filtering>
                <excludes>
                    <exclude>**/*.properties</exclude>
                    <exclude>**/*.yml</exclude>
                    <exclude>**/*.yaml</exclude>
                </excludes>
            </resource>
        </resources>
    </build>
</project>
```

### 6.2 模块POM示例
```xml
<!-- bone-core/bone-common/pom.xml -->
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.bone</groupId>
        <artifactId>bone-parent</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <artifactId>bone-common</artifactId>
    <name>Bone Common</name>
    <description>Common utilities for Bone platform</description>

    <dependencies>
        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>
        
        <!-- Apache Commons -->
        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
        </dependency>
        
        <!-- Jackson -->
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
        </dependency>
        
        <!-- Validation -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        
        <!-- 测试 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
```

---

## 🗄️ 数据库设计规范

### 7.1 表命名规范
```sql
-- 系统表：sys_前缀
CREATE TABLE sys_user (...);
CREATE TABLE sys_role (...);
CREATE TABLE sys_menu (...);

-- 业务表：biz_前缀  
CREATE TABLE biz_order (...);
CREATE TABLE biz_product (...);

-- 元数据表：meta_前缀
CREATE TABLE meta_entity (...);
CREATE TABLE meta_field (...);

-- 中间表：rel_前缀
CREATE TABLE rel_user_role (...);
```

### 7.2 字段命名规范
```sql
-- 主键字段
id BIGINT PRIMARY KEY AUTO_INCREMENT,

-- 时间字段
create_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
update_time TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

-- 状态字段
status TINYINT NOT NULL DEFAULT 1 COMMENT '状态：1-启用，0-禁用',

-- 软删除字段
is_deleted TINYINT NOT NULL DEFAULT 0 COMMENT '是否删除：1-是，0-否',

-- 预留扩展列
ext_col_1 VARCHAR(255) COMMENT '扩展字段1',
ext_col_2 VARCHAR(255) COMMENT '扩展字段2',
ext_col_3 BIGINT COMMENT '扩展字段3'
```

### 7.3 索引规范
```sql
-- 主键索引
PRIMARY KEY (id),

-- 唯一索引
UNIQUE KEY uk_username (username),
UNIQUE KEY uk_email (email),

-- 普通索引
INDEX idx_status (status),
INDEX idx_create_time (create_time),

-- 组合索引
INDEX idx_status_create_time (status, create_time)
```

---

## 🌐 API设计规范

### 8.1 RESTful API规范
```java
@RestController
@RequestMapping("/api/v1/users")
@Validated
@Slf4j
@RequiredArgsConstructor
public class UserController {
    
    private final UserService userService;
    
    /**
     * 创建用户
     */
    @PostMapping
    @Operation(summary = "创建用户")
    public Result<UserVO> createUser(@Valid @RequestBody UserCreateDTO createDTO) {
        UserVO userVO = userService.createUser(createDTO);
        return Result.success(userVO);
    }
    
    /**
     * 分页查询用户
     */
    @GetMapping
    @Operation(summary = "分页查询用户")
    public Result<PageResult<UserVO>> pageUsers(@Valid UserQueryDTO queryDTO) {
        PageResult<UserVO> pageResult = userService.pageUsers(queryDTO);
        return Result.success(pageResult);
    }
    
    /**
     * 获取用户详情
     */
    @GetMapping("/{userId}")
    @Operation(summary = "获取用户详情")
    public Result<UserVO> getUserById(@PathVariable Long userId) {
        UserVO userVO = userService.getUserById(userId);
        return Result.success(userVO);
    }
    
    /**
     * 更新用户
     */
    @PutMapping("/{userId}")
    @Operation(summary = "更新用户")
    public Result<UserVO> updateUser(@PathVariable Long userId, 
                                    @Valid @RequestBody UserUpdateDTO updateDTO) {
        UserVO userVO = userService.updateUser(userId, updateDTO);
        return Result.success(userVO);
    }
    
    /**
     * 删除用户
     */
    @DeleteMapping("/{userId}")
    @Operation(summary = "删除用户")
    public Result<Void> deleteUser(@PathVariable Long userId) {
        userService.deleteUser(userId);
        return Result.success();
    }
}
```

### 8.2 统一响应格式
```java
/**
 * 统一响应结果
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
public class Result<T> implements Serializable {
    
    private Boolean success;
    private Integer code;
    private String message;
    private T data;
    private Long timestamp;
    
    public static <T> Result<T> success() {
        return success(null);
    }
    
    public static <T> Result<T> success(T data) {
        return new Result<>(true, 200, "操作成功", data, System.currentTimeMillis());
    }
    
    public static <T> Result<T> error(String message) {
        return error(500, message);
    }
    
    public static <T> Result<T> error(Integer code, String message) {
        return new Result<>(false, code, message, null, System.currentTimeMillis());
    }
}

/**
 * 分页结果
 */
@Data
@NoArgsConstructor
@AllArgsConstructor
public class PageResult<T> implements Serializable {
    
    private List<T> records;
    private Long total;
    private Long size;
    private Long current;
    
    public static <T> PageResult<T> of(List<T> records, long total, long size, long current) {
        return new PageResult<>(records, total, size, current);
    }
}
```

---

## 🎯 总结

### 9.1 核心优势
- ✅ **架构清晰**：严格分层，职责明确
- ✅ **规范统一**：命名、代码、配置标准化
- ✅ **扩展性强**：SPI + 插件化设计
- ✅ **企业级就绪**：多租户、安全、监控内置
- ✅ **开发高效**：代码生成 + Starter自动配置

### 9.2 实施建议
1. **第一阶段**：搭建基础架构，实现核心模块
2. **第二阶段**：开发元数据引擎和ORM框架
3. **第三阶段**：构建平台服务和业务应用
4. **第四阶段**：完善工具链和文档体系

### 9.3 质量保证
- 代码规范检查（Checkstyle）
- 单元测试覆盖（JUnit 5）
- 集成测试（Testcontainers）
- 代码审查流程
- 持续集成（GitHub Actions）

> **"优秀的工程规范是高效协作的基石，持续改进是技术卓越的动力。"**

**Bone — Build Once, Natively Everywhere**  
**让企业级开发更简单、更规范、更高效**