# DDL 对齐说明（已废止）

> **本文已废止**（2026-05-16）。As-Is / Vision 双轨、Flyway 增量策略均已取消。  
> **请改用**：[数据库开发规范.md](./数据库开发规范.md) 与根目录 [`bone-init.sql`](../../bone-init.sql)。

---

历史读者须知：此前 `bone-init.sql` 与 `初始脚本.sql` Part B 不得混跑；现统一为 **仅执行 `bone-init.sql`** 作为开发库终态。
