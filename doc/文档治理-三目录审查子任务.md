# doc/prd · doc/design · doc/architecture 文档审查子任务

> **目标**：三目录内文案逻辑自洽、与仓库一致。  
> **DDL 唯一权威**：[数据库开发规范.md](./architecture/数据库开发规范.md)（含表清单）→ 根目录 `bone-init.sql`。

---

## 子任务总览

| ID | 范围 | 内容 | 状态 |
|----|------|------|------|
| **T1**～**T12** | 三目录 + DDL | 见历史修订 | ✅ |
| **T13** | DDL 单轨终局 | 最终规范、`bone-init.sql` 全量重写、删除 V1～V6 Flyway、废止双轨文档 | ✅ |
| **T14** | 规范合并 | 《数据库 DDL 最终规范》并入《数据库开发规范》；旧文件保留跳转 | ✅ |
| **T15** | 引用清理 | 废止跳转页；PRD/Studio/控制台/集成详设去除双轨 DDL 与过时链接 | ✅ |
| **T16** | 详设 §4 瘦身 | 元数据/主数据/系统/扩展/控制台/Generator/IAM 去除内嵌 SQL，指向 `bone-init` + 开发规范 | ✅ |
| **T17** | 方案 C 时间命名 | 全库 `created_at`/`updated_at`/`xxx_at`；`AbstractEntity` + init + platform 代码 | ✅ |

---

## DDL 决策（2026-05-16 终局）

| 项 | 结论 |
|----|------|
| 唯一脚本 | `bone-init.sql`（26 张表：IAM 10 + System 4 + Integration 7 + Meta 5） |
| 主键 | BIGINT Snowflake，禁止 AUTO_INCREMENT |
| 时间/操作人 | 统一 `created_at` / `updated_at` / `created_by` / `updated_by` 及业务 `xxx_at` |
| 集成执行表 | **`int_execution_log`**（非 `int_flow_execution`） |
| 变更 | 改 init → 重建库；`spring.flyway.enabled: false` |

---

## 后续（按模块落地时）

1. 新模块表（`mdm_*`、`ext_*`、`gen_*`、`cnsl_*` 等）直接追加到 `bone-init.sql` 并更新 [数据库开发规范.md](./architecture/数据库开发规范.md) §2。
2. 详设 §4 仅保留字段说明表，**不**再维护内嵌 `CREATE TABLE`。

---

**维护**：DDL 变更同步 [数据库开发规范.md](./architecture/数据库开发规范.md) §2 表清单。
