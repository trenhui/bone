# 9. SmartMeta 引擎模块技术说明

> **定位**：与仓库实现一致的 **技术骨架说明**（包职责、Maven 模块、核心入口）。  
> **与 SDK / server 的分工**：[元数据能力-实现映射与竞品对照.md](./元数据能力-实现映射与竞品对照.md)  
> **非目标**：替代源码；不包含大段示例实现代码（旧版长文已从仓库删除，见 Git 历史）。

---

## 1. Maven 模块（真相源：`bone-engine/bone-metadata-engine/pom.xml`）

| 模块 | Artifact | 说明 |
|------|------------|------|
| 引擎 | `bone-metadata-engine-core` | 元数据模型、查询与规则、动态数据访问、安全与缓存等核心能力 |
| 启动器 | `bone-metadata-engine-starter` | Spring Boot 自动配置、将引擎能力装配进宿主应用 |

> **已退役**：`bone-smartmeta-*`；包名 `com.bone.smartmeta.engine` → `com.bone.metadata.engine`。

当前聚合工程下 **仅上述两个子模块**；独立 SDK、CLI、Studio 前端等若在后续迭代出现，以对应 `pom.xml` 为准。

---

## 2. 根包与分层思想

引擎源码根包：

`bone-metadata-engine-core/src/main/java/com/bone/metadata/engine/`

设计取向（与 Bone 全局 DDD 叙述对齐，但本引擎以 **元数据运行时** 为重）：

- **metadata / model**：实体、字段、操作、工作流等元数据定义与注册表抽象。
- **service**：面向「通用操作 / 动态数据」的应用级编排（如 `GenericOperationService`、`DynamicDataService`）。
- **query**：SmartQL 解析、SQL 生成、执行与性能相关组件。
- **rule / expression / validation**：业务规则、表达式求值、元数据校验策略。
- **security**：字段级安全、权限评估、审计与脱敏等（部分为演示或扩展点，以类上注释为准）。
- **repository**：元数据仓储接口与内存等实现。
- **config**：`SmartMetaAutoConfiguration`、`SmartMetaProperties`、`RuleEngineConfig` 等 Spring 装配。
- **cache / tenant / multi / context**：缓存、租户与请求上下文。
- **annotation**：`@SmartEntity`、`@SmartField`、`@BusinessRule` 等声明式元数据入口。

**依赖建议（阅读代码时）**：从 `MetadataEngine`、`GenericOperationService`、`DynamicDataService` 入口向下追踪，比死记虚构包树更有效。

---

## 3. 子包职责速查（与当前目录一致）

下列为 `com.bone.metadata.engine` 下常见子包及职责摘要（随版本演进以 `bone-metadata-engine-core/src/main/java` 目录为准）：

| 子包 | 职责摘要 |
|------|----------|
| `annotation` | 实体/字段/规则等注解与类型枚举 |
| `metadata` | 元数据定义类、操作与工作流元数据、处理器协作 |
| `model` | 注册表模型、动态实体、规则结果等运行期模型 |
| `repository` | 元数据持久化/内存仓储抽象与实现 |
| `service` / `service/impl` | 通用操作、动态数据、规则引擎实现等 |
| `query` / `query/ast` | SmartQL、AST、SQL 生成、执行与缓存 |
| `expression` | 表达式求值 |
| `validation` / `validation/strategy` | 元数据与字段校验策略 |
| `rule` | 规则注册、求值上下文工厂等 |
| `security` | 权限、字段级安全、脱敏、演示用安全样例 |
| `cache` | 元数据缓存与统计 |
| `config` | 自动配置与引擎初始化 |
| `tenant` / `multi` / `context` | 租户与多租户上下文、用户上下文 |
| `event` | 元数据变更等事件发布抽象 |
| `exception` / `common` | 异常体系与公共常量 |
| `util` / `analysis` / `impact` / `version` | 工具、影响分析、版本控制等横切能力 |

---

## 4. 与文档体系的关系

| 文档 | 用途 |
|------|------|
| [`doc/architecture/README.md`](../../architecture/README.md) | 架构文档索引（总体、DDD、前端、数据库） |
| [`doc/architecture/Bone-DDD-最终实践方案.md`](../../architecture/Bone-DDD-最终实践方案.md) | 平台 DDD 与持久化 P0 门禁（业务模块适用；引擎内部包结构以源码为准） |
| [`doc/architecture/smartmeta/README.md`](../../architecture/smartmeta/README.md) | SmartMeta 补充导航 |
| [`bone-engine/bone-metadata-engine/`](../../../bone-engine/bone-metadata-engine/) | 引擎源码与 `pom.xml`（**唯一实现真源**） |

> `smartmeta/` 下长文可能仍含已退役包名；以本文件与源码为准，勿再新增与之冲突的独立 Markdown。

---

## 5. 维护约定

1. **包结构与类名**以 `bone-metadata-engine-core` 源码为准；本文件只描述稳定分层与入口，不复制大段 API。
2. 新增子包或拆分模块时：**先改代码**，再更新本节「子包职责速查」表。
3. 避免再新增「数千行虚构实现」的独立 Markdown；详细算法与接口契约优先落在 **JavaDoc + 单测**。

同级目录中另有控制台、元数据、主数据、集成、扩展、IAM、系统管理、Studio Generator 等模块详细设计，可按编号查阅。
