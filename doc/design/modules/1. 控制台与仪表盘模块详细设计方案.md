# 控制台与仪表盘模块详细设计方案

## 1. 模块概述

控制台与仪表盘模块是 BONE 平台的核心入口，负责系统概览、数据可视化、快速导航和操作中心，为用户提供直观的系统状态展示和快捷操作入口。

**核心目标**：
- 提供系统全局概览和健康状态监控
- 展示关键业务指标和数据趋势
- 提供快速导航和常用操作入口
- 支持自定义仪表盘和数据展示
- 实现多租户隔离的数据视图

## 2. 功能设计

### 2.1 系统概览

**功能描述**：展示系统的整体运行状态和健康情况，包括服务状态、资源使用、关键指标等。

**详细功能**：
- **服务健康状态**：显示各微服务的运行状态，包括在线状态、响应时间、错误率等
- **资源使用情况**：CPU、内存、磁盘等系统资源的使用情况
- **关键业务指标**：核心业务指标的实时展示，如订单数、用户数、交易金额等
- **系统告警**：显示最近的系统告警信息
- **快速导航**：提供常用功能的快速访问入口

**用户故事**：
> 作为系统管理员，我希望通过系统概览了解系统的整体运行状态，以便及时发现和解决问题。

### 2.2 数据仪表盘

**功能描述**：提供可自定义的数据可视化仪表盘，支持多种图表类型和数据来源。

**详细功能**：
- **图表类型**：支持折线图、柱状图、饼图、仪表盘等多种图表类型
- **数据来源**：支持从系统指标、业务数据、外部数据源等获取数据
- **自定义布局**：支持拖拽式自定义仪表盘布局
- **时间范围**：支持选择不同的时间范围查看数据
- **数据导出**：支持将仪表盘数据导出为PDF、Excel等格式

**用户故事**：
> 作为业务分析师，我希望创建自定义的数据仪表盘，以便直观地分析业务数据和趋势。

### 2.3 快速操作中心

**功能描述**：提供常用操作的快捷入口，简化用户操作流程。

**详细功能**：
- **常用功能**：提供系统中最常用功能的快速访问
- **最近操作**：显示用户最近的操作记录
- **快捷命令**：支持通过命令行方式执行常用操作
- **任务管理**：显示和管理当前正在执行的任务

**用户故事**：
> 作为普通用户，我希望通过快速操作中心快速访问常用功能，提高工作效率。

### 2.4 租户管理

**功能描述**：为多租户场景提供租户管理功能，包括租户概览、租户切换等。

**详细功能**：
- **租户概览**：显示当前租户的基本信息和资源使用情况
- **租户切换**：支持在不同租户之间快速切换
- **租户创建**：支持创建新的租户
- **租户配置**：管理租户的基本配置和权限

**用户故事**：
> 作为多租户管理员，我希望通过租户管理功能查看和管理不同租户的信息，以便更好地为租户提供服务。

## 3. 技术实现

### 3.1 前端技术

| 技术 | 版本 | 用途 |
|------|------|------|
| React | 18+ | 前端框架 |
| TypeScript | 5.5+ | 类型系统 |
| Ant Design | 5.12+ | UI组件库 |
| ECharts | 5.4+ | 数据可视化 |
| Redux Toolkit | 2.0+ | 状态管理 |
| React Router | 6.20+ | 路由管理 |
| axios | 1.6+ | HTTP客户端 |

### 3.2 后端技术

| 技术 | 版本 | 用途 |
|------|------|------|
| Java | 17+ | 核心开发语言 |
| Spring Boot | 3.2+ | 后端框架 |
| Spring Security | 6.2+ | 安全框架 |
| Prometheus | 2.45+ | 监控系统 |
| Grafana | 10.0+ | 监控面板 |
| Redis | 7.0+ | 缓存 |
| MySQL | 8.0+ | 关系型数据库 |

### 3.3 关键实现

#### 3.3.1 系统概览实现

```java
package com.bone.console.application.query.handler;

import com.bone.console.application.query.qry.SystemOverviewQry;
import com.bone.console.application.query.dto.SystemOverviewDTO;
import com.bone.console.application.query.dto.ServiceStatusDTO;
import com.bone.console.application.query.dto.ResourceUsageDTO;
import com.bone.console.domain.gateway.MonitoringGateway;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Component;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;

@Component
@RequiredArgsConstructor
public class SystemOverviewQueryHandler {
    private final MonitoringGateway monitoringGateway;
    
    @Transactional(readOnly = true)
    public SystemOverviewDTO handle(SystemOverviewQry qry) {
        List<ServiceStatusDTO> serviceStatuses = monitoringGateway.getServiceStatuses();
        ResourceUsageDTO resourceUsage = monitoringGateway.getResourceUsage();
        
        SystemOverviewDTO overview = new SystemOverviewDTO();
        overview.setServiceStatuses(serviceStatuses);
        overview.setResourceUsage(resourceUsage);
        
        return overview;
    }
}
```

#### 3.3.2 仪表盘实现

```java
package com.bone.console.application.command.handler;

import com.bone.console.application.command.cmd.CreateDashboardCmd;
import com.bone.console.domain.model.Dashboard;
import com.bone.console.domain.model.widget.Widget;
import com.bone.console.domain.repository.DashboardRepository;
import com.bone.console.common.exception.BusinessException;
import lombok.RequiredArgsConstructor;
import org.springframework.stereotype.Component;
import org.springframework.transaction.annotation.Transactional;

import java.util.List;

@Component
@RequiredArgsConstructor
public class CreateDashboardHandler {
    private final DashboardRepository dashboardRepository;
    
    @Transactional
    public Long handle(CreateDashboardCmd cmd) {
        // 验证仪表盘名称是否已存在
        if (dashboardRepository.existsByName(cmd.getName())) {
            throw new BusinessException("仪表盘名称已存在");
        }
        
        Dashboard dashboard = Dashboard.create(
            cmd.getId(),
            cmd.getTenantId(),
            cmd.getName(),
            cmd.getDescription(),
            cmd.getWidgets()
        );
        
        dashboardRepository.save(dashboard);
        
        return dashboard.getId().getValue();
    }
}
```

## 4. 数据模型

### 4.1 核心表结构

| 表名 | 描述 | 关键字段 |
|------|------|----------|
| `cnsl_dashboard_widget` | 仪表盘Widget配置表 | id, tenant_id, user_id, type, config, position_x, position_y, width, height, is_enabled, sort_order |
| `cnsl_notification` | 通知表 | id, tenant_id, user_id, type, title, content, action_url, is_read, read_at, source_module, source_event_id |
| `cnsl_recent_access` | 最近访问记录表 | id, tenant_id, user_id, resource_type, resource_id, resource_name, access_url, accessed_at |

### 4.2 完整建表 SQL（MySQL 8.0+）

```sql
-- ===========================================================
-- 控制台与仪表盘模块完整建表SQL
-- 数据库：bone
-- 引擎：InnoDB, 字符集：utf8mb4
-- 遵循 doc/architecture/Bone-DDD-最终实践方案.md（第二部分）与主 PRD 数据约定：
--   主键使用 BIGINT（雪花算法）
--   软删除：deleted TINYINT(1)
--   乐观锁：version INT
--   审计：created_by, updated_by, created_at, updated_at
--   租户：tenant_id
-- ===========================================================

-- 仪表盘Widget配置表
CREATE TABLE cnsl_dashboard_widget (
    id                  BIGINT          NOT NULL COMMENT 'Widget主键（Snowflake）',
    tenant_id           BIGINT          NOT NULL COMMENT '租户ID',
    user_id             BIGINT          NOT NULL COMMENT '用户ID',
    type                VARCHAR(50)     NOT NULL COMMENT 'Widget类型',
    config              JSON            NOT NULL COMMENT 'Widget配置',
    position_x          INT             NOT NULL DEFAULT 0 COMMENT 'X轴位置',
    position_y          INT             NOT NULL DEFAULT 0 COMMENT 'Y轴位置',
    width               INT             NOT NULL DEFAULT 4 COMMENT '宽度（栅格）',
    height              INT             NOT NULL DEFAULT 1 COMMENT '高度（行）',
    is_enabled          TINYINT(1)      NOT NULL DEFAULT 1 COMMENT '是否启用：0-禁用，1-启用',
    sort_order          INT             NOT NULL DEFAULT 0 COMMENT '排序号',
    created_by          BIGINT          DEFAULT NULL COMMENT '创建人ID',
    updated_by          BIGINT          DEFAULT NULL COMMENT '修改人ID',
    created_at          DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) COMMENT '创建时间',
    updated_at          DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) ON UPDATE CURRENT_TIMESTAMP(3) COMMENT '更新时间',
    deleted             TINYINT(1)      NOT NULL DEFAULT 0 COMMENT '逻辑删除：0-未删除，1-已删除',
    PRIMARY KEY (id),
    KEY idx_cnsl_dw_user (user_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_bin COMMENT='仪表盘Widget配置表';

-- 通知表
CREATE TABLE cnsl_notification (
    id                  BIGINT          NOT NULL COMMENT '通知主键（Snowflake）',
    tenant_id           BIGINT          NOT NULL COMMENT '租户ID',
    user_id             BIGINT          NOT NULL COMMENT '用户ID',
    type                VARCHAR(50)     NOT NULL COMMENT '通知类型',
    title               VARCHAR(200)    NOT NULL COMMENT '通知标题',
    content             TEXT            DEFAULT NULL COMMENT '通知内容',
    action_url          VARCHAR(500)    DEFAULT NULL COMMENT '操作链接',
    is_read             TINYINT(1)      NOT NULL DEFAULT 0 COMMENT '是否已读：0-否，1-是',
    read_at             DATETIME(3)     DEFAULT NULL COMMENT '阅读时间',
    source_module       VARCHAR(50)     NOT NULL COMMENT '来源模块',
    source_event_id     VARCHAR(64)     DEFAULT NULL COMMENT '来源事件ID',
    created_by          BIGINT          DEFAULT NULL COMMENT '创建人ID',
    created_at          DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) COMMENT '创建时间',
    PRIMARY KEY (id),
    KEY idx_cnsl_notif_user_read (user_id, is_read),
    KEY idx_cnsl_notif_time (created_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_bin COMMENT='通知表';

-- 最近访问记录表
CREATE TABLE cnsl_recent_access (
    id                  BIGINT          NOT NULL COMMENT '访问记录主键（Snowflake）',
    tenant_id           BIGINT          NOT NULL COMMENT '租户ID',
    user_id             BIGINT          NOT NULL COMMENT '用户ID',
    resource_type       VARCHAR(50)     NOT NULL COMMENT '资源类型',
    resource_id         VARCHAR(200)    NOT NULL COMMENT '资源ID',
    resource_name       VARCHAR(200)    NOT NULL COMMENT '资源名称',
    access_url          VARCHAR(500)    NOT NULL COMMENT '访问URL',
    accessed_at         DATETIME(3)     NOT NULL DEFAULT CURRENT_TIMESTAMP(3) COMMENT '访问时间',
    PRIMARY KEY (id),
    KEY idx_cnsl_ra_user (user_id, accessed_at)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_bin COMMENT='最近访问记录表';
```

## 5. API 设计

### 5.1 系统概览 API

| API路径 | 方法 | 功能 | 权限 |
|---------|------|------|------|
| `/api/console/overview` | GET | 获取系统概览 | 系统管理权限 |
| `/api/console/services` | GET | 获取服务状态 | 系统管理权限 |
| `/api/console/resources` | GET | 获取资源使用情况 | 系统管理权限 |
| `/api/console/metrics` | GET | 获取关键指标 | 系统管理权限 |

### 5.2 仪表盘 API

| API路径 | 方法 | 功能 | 权限 |
|---------|------|------|------|
| `/api/console/dashboards` | GET | 获取仪表盘列表 | 仪表盘管理权限 |
| `/api/console/dashboards` | POST | 创建仪表盘 | 仪表盘管理权限 |
| `/api/console/dashboards/{id}` | GET | 获取仪表盘详情 | 仪表盘管理权限 |
| `/api/console/dashboards/{id}` | PUT | 更新仪表盘 | 仪表盘管理权限 |
| `/api/console/dashboards/{id}` | DELETE | 删除仪表盘 | 仪表盘管理权限 |
| `/api/console/dashboards/{id}/widgets` | GET | 获取仪表盘组件 | 仪表盘管理权限 |
| `/api/console/dashboards/{id}/widgets` | POST | 添加仪表盘组件 | 仪表盘管理权限 |

### 5.3 组件 API

| API路径 | 方法 | 功能 | 权限 |
|---------|------|------|------|
| `/api/console/widgets/{id}` | GET | 获取组件详情 | 仪表盘管理权限 |
| `/api/console/widgets/{id}` | PUT | 更新组件 | 仪表盘管理权限 |
| `/api/console/widgets/{id}` | DELETE | 删除组件 | 仪表盘管理权限 |
| `/api/console/widgets/{id}/data` | GET | 获取组件数据 | 仪表盘管理权限 |
| `/api/console/widgets/{id}/data` | POST | 刷新组件数据 | 仪表盘管理权限 |

### 5.4 用户偏好 API

| API路径 | 方法 | 功能 | 权限 |
|---------|------|------|------|
| `/api/console/preferences` | GET | 获取用户偏好 | 登录用户 |
| `/api/console/preferences` | PUT | 更新用户偏好 | 登录用户 |

### 5.5 快速操作 API

| API路径 | 方法 | 功能 | 权限 |
|---------|------|------|------|
| `/api/console/quick-actions` | GET | 获取快速操作列表 | 登录用户 |
| `/api/console/quick-actions` | POST | 创建快速操作 | 系统管理权限 |
| `/api/console/quick-actions/{id}` | PUT | 更新快速操作 | 系统管理权限 |
| `/api/console/quick-actions/{id}` | DELETE | 删除快速操作 | 系统管理权限 |

## 6. 部署与集成

### 6.1 部署方式

- **容器化部署**：使用 Docker 容器部署，通过 Kubernetes 进行编排
- **资源需求**：
  - CPU：1 核
  - 内存：1GB
  - 存储：10GB

### 6.2 集成点

- **与监控系统集成**：集成 Prometheus 和 Grafana，获取系统监控数据
- **与业务系统集成**：从各业务系统获取业务指标数据
- **与认证系统集成**：使用统一的认证和权限管理
- **与配置中心集成**：使用 Nacos 配置中心管理配置

### 6.3 配置项

| 配置项 | 类型 | 默认值 | 描述 |
|--------|------|--------|------|
| `console.dashboard.max.widgets` | Integer | 20 | 每个仪表盘最大组件数 |
| `console.widget.refresh.interval` | Integer | 60 | 默认组件刷新间隔（秒） |
| `console.data.retention.days` | Integer | 30 | 数据保留天数 |
| `console.system.overview.update.interval` | Integer | 60 | 系统概览更新间隔（秒） |

## 7. 性能与安全

### 7.1 性能优化

- **缓存策略**：使用 Redis 缓存系统概览和仪表盘数据，减少数据库查询
- **异步处理**：组件数据刷新使用异步处理，避免阻塞主线程
- **数据聚合**：对历史数据进行聚合，减少数据存储和查询压力
- **分页查询**：对大量数据使用分页查询，提高响应速度
- **连接池**：使用数据库连接池和 HTTP 连接池，提高连接效率

### 7.2 安全措施

- **权限控制**：基于 RBAC 的细粒度权限控制
- **数据隔离**：多租户数据隔离，确保数据安全
- **传输加密**：使用 TLS 1.3 加密所有网络传输
- **输入验证**：严格的输入验证，防止注入攻击
- **审计日志**：记录所有仪表盘和组件操作，支持审计追踪

## 8. 测试计划

### 8.1 单元测试

- **测试范围**：核心服务和工具类
- **测试工具**：JUnit 5 + Mockito
- **覆盖率目标**：≥85%

### 8.2 集成测试

- **测试范围**：API 接口和服务间集成
- **测试工具**：Spring Boot Test
- **覆盖率目标**：≥65%

### 8.3 端到端测试

- **测试范围**：完整用户流程
- **测试工具**：Selenium + TestNG
- **测试场景**：
  - 系统概览展示
  - 仪表盘创建和编辑
  - 组件数据刷新
  - 用户偏好设置
  - 快速操作使用

## 9. 监控与告警

### 9.1 监控指标

- **系统指标**：CPU 使用率、内存使用率、磁盘使用率
- **应用指标**：API 响应时间、错误率、QPS
- **业务指标**：仪表盘加载时间、组件刷新成功率

### 9.2 告警机制

- **告警规则**：基于 Prometheus 告警规则
- **告警级别**：严重、警告、信息
- **告警渠道**：邮件、短信、企业微信
- **告警场景**：
  - 仪表盘加载失败
  - 组件数据刷新失败
  - 系统资源使用异常

### 9.3 日志管理

- **日志内容**：系统操作日志、错误日志、性能日志
- **日志存储**：使用 ELK Stack 收集和分析日志
- **日志保留期**：30 天

## 10. 微服务与Java工程结构

### 10.1 微服务设计

**微服务名称**：console-service

**服务职责**：
- 系统概览数据收集和展示
- 仪表盘管理和配置
- 组件数据处理和刷新
- 用户偏好设置管理
- 快速操作管理

**API前缀**：`/api/v1/console`

**数据库**：`console_db`

**端口（规划示例）**：8081 — 当前仓库**尚无**独立 `bone-console` 可部署单元；若落地控制台服务，**勿与 `bone-iam` 默认 8081 冲突**。真源：[doc/wiki/03-本地开发与构建.md](../../wiki/03-本地开发与构建.md)「常见服务端口」。

### 10.2 Java工程结构

与 [`doc/architecture/Bone-DDD-最终实践方案.md`](../../architecture/Bone-DDD-最终实践方案.md) **§14** 对齐：六边形 + **CQRS**（`command`/`query` + Handler）；**bone-metadata-sdk** 持久化。「Bone-Blueprint」指 `bone-blueprint` 示例仓库，**不以 Blueprint 文档版本号为准**；UseCase 目录非必需（§20）。

```
bone-console/
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── com/
│   │   │       └── bone/
│   │   │           └── console/
│   │   │               ├── ConsoleApplication.java  # 应用入口
│   │   │               │
│   │   │               ├── adapter/               # 入站适配器层
│   │   │               │   ├── web/
│   │   │               │   │   ├── controller/    # 控制器
│   │   │               │   │   │   ├── SystemOverviewController.java
│   │   │               │   │   │   ├── DashboardController.java
│   │   │               │   │   │   ├── WidgetController.java
│   │   │               │   │   │   ├── PreferenceController.java
│   │   │               │   │   │   └── QuickActionController.java
│   │   │               │   │   ├── dto/          # DTO
│   │   │               │   │   │   ├── request/    # 请求DTO
│   │   │               │   │   │   │   ├── CreateDashboardRequest.java
│   │   │               │   │   │   │   ├── UpdateWidgetRequest.java
│   │   │               │   │   │   │   └── UpdatePreferenceRequest.java
│   │   │               │   │   │   └── response/   # 响应DTO
│   │   │               │   │   │       ├── SystemOverviewResponse.java
│   │   │               │   │   │       ├── DashboardListResponse.java
│   │   │               │   │   │       └── WidgetDataResponse.java
│   │   │               │   │   └── assembler/    # 装配器
│   │   │               │   │       ├── DashboardWebAssembler.java
│   │   │               │   │       ├── WidgetWebAssembler.java
│   │   │               │   │       └── PreferenceWebAssembler.java
│   │   │               │   └── schedule/         # 定时任务
│   │   │               │       └── SystemOverviewUpdateJob.java
│   │   │               │
│   │   │               ├── application/            # 应用层
│   │   │               │   ├── usecase/           # 用例层
│   │   │               │   │   ├── simple/         # Mode A 简单模式
│   │   │               │   │   │   ├── GetSystemOverviewUseCase.java
│   │   │               │   │   │   ├── GetDashboardListUseCase.java
│   │   │               │   │   │   └── GetUserPreferenceUseCase.java
│   │   │               │   │   └── standard/       # Mode B 企业模式
│   │   │               │   │       ├── CreateDashboardUseCase.java
│   │   │               │   │       ├── UpdateDashboardUseCase.java
│   │   │               │   │       ├── CreateWidgetUseCase.java
│   │   │               │   │       └── RefreshWidgetDataUseCase.java
│   │   │               │   ├── command/           # 命令
│   │   │               │   │   ├── cmd/            # 命令对象
│   │   │               │   │   │   ├── CreateDashboardCmd.java
│   │   │               │   │   │   ├── UpdateDashboardCmd.java
│   │   │               │   │   │   ├── CreateWidgetCmd.java
│   │   │               │   │   │   └── RefreshWidgetDataCmd.java
│   │   │               │   │   └── handler/        # 命令处理器
│   │   │               │   │       ├── CreateDashboardHandler.java
│   │   │               │   │       ├── UpdateDashboardHandler.java
│   │   │               │   │       ├── CreateWidgetHandler.java
│   │   │               │   │       └── RefreshWidgetDataHandler.java
│   │   │               │   ├── query/             # 查询
│   │   │               │   │   ├── qry/            # 查询对象
│   │   │               │   │   │   ├── SystemOverviewQry.java
│   │   │               │   │   │   ├── DashboardPageQry.java
│   │   │               │   │   │   └── WidgetDataQry.java
│   │   │               │   │   ├── handler/        # 查询处理器
│   │   │               │   │   │   ├── SystemOverviewQueryHandler.java
│   │   │               │   │   │   ├── DashboardPageQueryHandler.java
│   │   │               │   │   │   └── WidgetDataQueryHandler.java
│   │   │               │   │   └── dto/            # 查询结果DTO
│   │   │               │   │       ├── SystemOverviewDTO.java
│   │   │               │   │       ├── DashboardDTO.java
│   │   │               │   │       ├── WidgetDTO.java
│   │   │               │   │       └── WidgetDataDTO.java
│   │   │               │   └── event/             # 事件处理器
│   │   │               │       ├── DashboardCreatedHandler.java
│   │   │               │       └── WidgetDataRefreshedHandler.java
│   │   │               │
│   │   │               ├── domain/                # 领域层（零依赖）
│   │   │               │   ├── dashboard/         # 仪表盘领域
│   │   │               │   │   ├── Dashboard.java           # 聚合根
│   │   │               │   │   ├── Widget.java              # 聚合根
│   │   │               │   │   ├── valueobject/          # 值对象
│   │   │               │   │   │   ├── DashboardId.java
│   │   │               │   │   │   ├── WidgetId.java
│   │   │               │   │   │   ├── WidgetType.java
│   │   │               │   │   │   └── LayoutConfig.java
│   │   │               │   │   └── event/                # 领域事件
│   │   │               │   │       ├── DashboardCreatedEvent.java
│   │   │               │   │       ├── DashboardUpdatedEvent.java
│   │   │               │   │       └── WidgetAddedEvent.java
│   │   │               │   ├── preference/        # 用户偏好领域
│   │   │               │   │   ├── UserPreference.java       # 聚合根
│   │   │               │   │   ├── valueobject/          # 值对象
│   │   │               │   │   │   ├── PreferenceId.java
│   │   │               │   │   │   └── ThemeType.java
│   │   │               │   │   └── event/                # 领域事件
│   │   │               │   │       └── PreferenceUpdatedEvent.java
│   │   │               │   ├── repository/       # 仓储接口（空接口）
│   │   │               │   │   ├── DashboardRepository.java
│   │   │               │   │   ├── WidgetRepository.java
│   │   │               │   │   └── UserPreferenceRepository.java
│   │   │               │   ├── gateway/           # 防腐层接口
│   │   │               │   │   ├── MonitoringGateway.java
│   │   │               │   │   ├── BusinessDataGateway.java
│   │   │               │   │   └── ExternalDataSourceGateway.java
│   │   │               │   └── service/          # 领域服务
│   │   │               │       ├── DashboardService.java
│   │   │               │       └── WidgetService.java
│   │   │               │
│   │   │               ├── infrastructure/       # 基础设施层
│   │   │               │   ├── gateway/           # 网关实现
│   │   │               │   │   ├── PrometheusMonitoringGatewayImpl.java
│   │   │               │   │   ├── BusinessDataGatewayImpl.java
│   │   │               │   │   └── RestDataSourceGatewayImpl.java
│   │   │               │   ├── query/native/     # 原生查询
│   │   │               │   │   └── WidgetDataNativeQuery.java
│   │   │               │   └── config/           # 配置
│   │   │               │       ├── SecurityConfig.java
│   │   │               │       ├── SwaggerConfig.java
│   │   │               │       └── SchedulingConfig.java
│   │   │               │
│   │   │               └── common/               # 应用级通用组件
│   │   │                   ├── exception/        # 异常
│   │   │                   │   ├── BusinessException.java
│   │   │                   │   ├── NotFoundException.java
│   │   │                   │   └── SystemException.java
│   │   │                   ├── result/           # 响应
│   │   │                   │   ├── ApiResponse.java
│   │   │                   │   └── PageResult.java
│   │   │                   └── util/             # 工具
│   │   │                       ├── DashboardUtils.java
│   │   │                       └── WidgetDataUtils.java
│   │   └── resources/
│   │       ├── application.yml
│   │       ├── application-dev.yml
│   │       └── application-prod.yml
│   └── test/            # 测试代码
│       └── java/
│           └── com/
│               └── bone/
│                   └── console/
│                       ├── adapter/
│                       ├── application/
│                       ├── domain/
│                       └── infrastructure/
├── pom.xml             # Maven配置
├── README.md           # 服务说明
└── Dockerfile          # Docker构建文件
```

### 10.3 核心类设计

#### 10.3.1 控制器类（Adapter层）

**SystemOverviewController.java**
- 管理系统概览相关API
- 处理系统概览、服务状态、资源使用等查询
- 位于 `adapter/web/controller/` 目录
- 命名规范：`{Domain}Controller`

**DashboardController.java**
- 管理仪表盘相关API
- 处理仪表盘的创建、查询、更新、删除操作
- 位于 `adapter/web/controller/` 目录
- 命名规范：`{Domain}Controller`

**WidgetController.java**
- 管理组件相关API
- 处理组件的创建、查询、更新、删除操作
- 位于 `adapter/web/controller/` 目录
- 命名规范：`{Domain}Controller`

#### 10.3.2 UseCase类（Application层）

**GetSystemOverviewUseCase.java**（Mode A）
- 处理获取系统概览的简单用例
- 位于 `application/usecase/simple/` 目录
- 命名规范：`{Action}{Domain}UseCase`

**CreateDashboardUseCase.java**（Mode B）
- 处理创建仪表盘的复杂用例
- 位于 `application/usecase/standard/` 目录
- 命名规范：`{Action}{Domain}UseCase`

**RefreshWidgetDataUseCase.java**（Mode B）
- 处理刷新组件数据的复杂用例
- 位于 `application/usecase/standard/` 目录
- 命名规范：`{Action}{Domain}UseCase`

#### 10.3.3 命令和查询处理器（Application层）

**CreateDashboardHandler.java**
- 处理创建仪表盘的命令
- 位于 `application/command/handler/` 目录
- 命名规范：`{Verb}{Domain}Handler`
- 必须使用 `@Transactional` 注解

**RefreshWidgetDataHandler.java**
- 处理刷新组件数据的命令
- 位于 `application/command/handler/` 目录
- 命名规范：`{Verb}{Domain}Handler`
- 必须使用 `@Transactional` 注解

**SystemOverviewQueryHandler.java**
- 处理系统概览查询
- 位于 `application/query/handler/` 目录
- 命名规范：`{Domain}{Action}QueryHandler`
- 使用 `@Transactional(readOnly = true)` 注解

#### 10.3.4 领域模型（Domain层）

**Dashboard.java**（聚合根）
- 仪表盘领域模型
- 包含名称、描述、布局配置等属性
- 继承 `bone-core` 的 `AggregateRoot`
- 仅使用 `@Getter` 和私有构造器
- 位于 `domain/dashboard/` 目录
- 命名规范：`{Domain}`

**Widget.java**（聚合根）
- 组件领域模型
- 包含名称、类型、配置、位置等属性
- 继承 `bone-core` 的 `AggregateRoot`
- 仅使用 `@Getter` 和私有构造器
- 位于 `domain/dashboard/` 目录
- 命名规范：`{Domain}`

**UserPreference.java**（聚合根）
- 用户偏好领域模型
- 包含主题、语言、上次登录仪表盘等属性
- 继承 `bone-core` 的 `AggregateRoot`
- 仅使用 `@Getter` 和私有构造器
- 位于 `domain/preference/` 目录
- 命名规范：`{Domain}`

#### 10.3.5 领域事件（Domain层）

**DashboardCreatedEvent.java**
- 仪表盘创建事件
- 实现 `bone-core` 的 `DomainEvent`
- 位于 `domain/dashboard/event/` 目录
- 命名规范：`{Domain}{PastVerb}Event`

**WidgetDataRefreshedEvent.java**
- 组件数据刷新事件
- 实现 `bone-core` 的 `DomainEvent`
- 位于 `domain/dashboard/event/` 目录
- 命名规范：`{Domain}{PastVerb}Event`

#### 10.3.6 仓储接口（Domain层）

**DashboardRepository.java**
- 仪表盘仓储接口
- 继承 `bone-metadata-sdk` 的 `Repository`
- 空接口，不添加任何自定义方法
- 位于 `domain/repository/` 目录
- 命名规范：`{Aggregate}Repository`

**WidgetRepository.java**
- 组件仓储接口
- 继承 `bone-metadata-sdk` 的 `Repository`
- 空接口，不添加任何自定义方法
- 位于 `domain/repository/` 目录
- 命名规范：`{Aggregate}Repository`

**UserPreferenceRepository.java**
- 用户偏好仓储接口
- 继承 `bone-metadata-sdk` 的 `Repository`
- 空接口，不添加任何自定义方法
- 位于 `domain/repository/` 目录
- 命名规范：`{Aggregate}Repository`

#### 10.3.7 防腐层接口（Domain层）

**MonitoringGateway.java**
- 监控网关接口
- 用于获取系统监控数据
- 位于 `domain/gateway/` 目录
- 命名规范：`{ExternalSystem}Gateway`

**BusinessDataGateway.java**
- 业务数据网关接口
- 用于获取业务指标数据
- 位于 `domain/gateway/` 目录
- 命名规范：`{ExternalSystem}Gateway`

**ExternalDataSourceGateway.java**
- 外部数据源网关接口
- 用于从外部系统获取数据
- 位于 `domain/gateway/` 目录
- 命名规范：`{ExternalSystem}Gateway`

#### 10.3.8 网关实现（Infrastructure层）

**PrometheusMonitoringGatewayImpl.java**
- Prometheus监控网关实现
- 位于 `infrastructure/gateway/` 目录
- 命名规范：`{ExternalSystem}GatewayImpl`

**BusinessDataGatewayImpl.java**
- 业务数据网关实现
- 位于 `infrastructure/gateway/` 目录
- 命名规范：`{ExternalSystem}GatewayImpl`

**RestDataSourceGatewayImpl.java**
- REST数据源网关实现
- 位于 `infrastructure/gateway/` 目录
- 命名规范：`{ExternalSystem}GatewayImpl`

### 10.4 AI能力与扩展点

#### 10.4.1 AI能力定义

```java
@Capability(
    name = "dashboard.autoLayout",
    description = "智能仪表盘布局，根据组件类型和数据特性自动调整布局",
    version = "1.0.0"
)
@Service
public class DashboardAutoLayoutCapability {
    
    // 智能布局推荐
    public LayoutConfig recommendLayout(List<Widget> widgets) {
        // 使用AI分析组件类型和数据特性，推荐最佳布局
    }
    
    // 组件数据可视化建议
    public List<WidgetSuggestion> suggestWidgets(String dashboardPurpose) {
        // 根据仪表盘用途推荐合适的组件
    }
    
    // 异常数据检测
    public List<AnomalyDetection> detectAnomalies(List<WidgetData> widgetData) {
        // 使用AI检测异常数据
    }
}
```

#### 10.4.2 扩展点设计

```java
public interface WidgetDataCalculator {
    
    /**
     * 计算组件数据
     */
    WidgetData calculateData(Widget widget, QueryConfig config);
    
    /**
     * 计算组件数据刷新频率
     */
    int calculateRefreshInterval(Widget widget);
    
    /**
     * 计算组件优先级
     */
    int calculatePriority(Widget widget);
}
```

### 10.5 与其他微服务集成

**服务依赖**：
- **authz-service**：用于权限验证和用户认证
- **monitoring-service**：用于获取系统监控数据
- **business-services**：用于获取业务指标数据

**集成方式**：
- **Gateway**：通过 `domain/gateway/` 目录下的网关接口调用其他微服务
- **消息队列**：通过领域事件机制接收其他服务的事件
- **共享缓存**：通过Redis共享跨服务数据
- **API网关**：通过API网关统一调用其他微服务

**集成流程**：
1. **权限验证**：调用authz-service进行用户认证和权限检查
2. **数据获取**：调用monitoring-service获取系统监控数据
3. **业务数据**：调用business-services获取业务指标数据
4. **服务发现**：通过Nacos服务发现机制找到其他微服务

### 10.6 部署配置

> **说明**：以下 Dockerfile / K8s 片段为**目标态示例**；当前仓库无 `bone-console` 制品。端口 **8081** 仅为示意，落地时须避开 `bone-iam` 默认端口，并以根 README 端口表为准。

**Dockerfile**：
```dockerfile
FROM openjdk:17-jdk-alpine
WORKDIR /app
COPY target/bone-console.jar app.jar
EXPOSE 8081
ENTRYPOINT ["java", "-jar", "app.jar"]
```

**Kubernetes部署**：
```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: console-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: console-service
  template:
    metadata:
      labels:
        app: console-service
    spec:
      containers:
      - name: console-service
        image: bone-console:latest
        ports:
        - containerPort: 8081
        env:
        - name: SPRING_PROFILES_ACTIVE
          value: "prod"
        - name: DB_HOST
          value: "mysql"
        - name: DB_PORT
          value: "3306"
        - name: NACOS_SERVER_ADDR
          value: "nacos:8848"
        - name: REDIS_HOST
          value: "redis"
        - name: REDIS_PORT
          value: "6379"
        resources:
          requests:
            cpu: "1000m"
            memory: "1Gi"
          limits:
            cpu: "2000m"
            memory: "2Gi"
```

**服务配置**：
```yaml
apiVersion: v1
kind: Service
metadata:
  name: console-service
spec:
  selector:
    app: console-service
  ports:
  - port: 8081
    targetPort: 8081
  type: ClusterIP
```

**Ingress配置**：
```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: console-service-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: bone.example.com
    http:
      paths:
      - path: /api/v1/console
        pathType: Prefix
        backend:
          service:
            name: console-service
            port:
              number: 8081
```

## 11. 总结

控制台与仪表盘模块是 BONE 平台的核心入口，通过提供系统概览、数据可视化、快速操作和租户管理等功能，为用户提供了直观、高效的系统管理体验。该模块采用了先进的技术栈和架构设计，支持复杂的仪表盘和组件管理需求。

**核心优势**：
- **直观的系统概览**：实时展示系统运行状态和健康情况
- **灵活的仪表盘**：支持自定义仪表盘和多种图表类型
- **高效的快速操作**：提供常用功能的快捷访问
- **多租户支持**：实现租户隔离的数据视图
- **智能布局**：AI 辅助的仪表盘布局和组件推荐

**应用场景**：
- 系统运维：监控系统运行状态，及时发现和解决问题
- 业务分析：通过数据可视化分析业务趋势
- 快速操作：通过快捷入口提高工作效率
- 租户管理：管理多租户环境下的系统资源

控制台与仪表盘模块的设计充分考虑了用户体验、性能和可扩展性，为 BONE 平台提供了强大的系统管理能力，使平台能够更加直观、高效地运行和管理。

**符合规范**：
- 遵循《Bone-DDD》分层与 CQRS Handler 约定
- 应用层以 CommandHandler / QueryHandler 为入口（《Bone-DDD》§14、§20）
- 采用六边形架构，依赖方向正确
- 领域层纯净，仅使用 `@Getter` 和私有构造器
- 仓储接口为空接口，继承 `bone-metadata-sdk` 的 `Repository`
- 数据库表结构符合数据库规范，包含完整的中文注释
- 命名规范符合最新标准
- 集成AI能力定义（@Capability）
- 提供扩展点机制（Calculator命名）
