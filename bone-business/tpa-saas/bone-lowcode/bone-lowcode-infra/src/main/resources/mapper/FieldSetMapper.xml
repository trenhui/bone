<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bone.lowcode.infra.infrastructure.persistence.mapper.FieldSetMapper">

    <insert id="batchSaveWithId">
        insert into cfg_fieldset (id, page_id, container_id, code,
                                  name, model_id, deleted,
                                  create_by, create_time, update_by,
                                  update_time)
        values
        <foreach collection="fieldsetDOList" item="i" separator=",">
            (#{i.id,jdbcType=BIGINT}, #{i.pageId}, #{i.containerId,jdbcType=BIGINT}, #{i.code,jdbcType=VARCHAR},
            #{i.name,jdbcType=VARCHAR}, #{i.modelId,jdbcType=BIGINT}, #{i.deleted,jdbcType=TINYINT},
            #{i.createBy,jdbcType=BIGINT}, #{i.createTime,jdbcType=TIMESTAMP}, #{i.updateBy,jdbcType=BIGINT},
            #{i.updateTime,jdbcType=TIMESTAMP})
        </foreach>
    </insert>
</mapper>
