id: findUsersByRole
sql: |
  SELECT u.id, u.name, u.role_id AS roleId
  FROM users u
  LEFT JOIN roles r ON u.role_id = r.id
  <where>
    <if test="roleId != null">
      AND u.role_id LIKE :roleId
    </if>
  </where>
mappedClassName: com.bone.metadata.sdk.test.domain.dto.UserRoleDTO