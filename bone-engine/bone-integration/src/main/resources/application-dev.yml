server:
  port: 30888

#liteflow:
#  rule-source: classpath:liteflow/liteflow-rules-flow.xml
#  enable-log: true  # Enable logging for debugging purposes
#  enable-monitor: false
#  enable-cache: true

# Thread pool configuration
threadpool:
  corePoolSize: 20
  maxPoolSize: 100
  keepAliveTime: 60
  queueCapacity: 1000

metadata:
  sdk:
    appcode: tpa
    open: true
    secret: REDACTED_METADATA_SDK_SECRET==
    url: http://127.0.0.1:8081
    sqlpath: customsql/

mybatis:
  mapper-locations: classpath:repository/*.xml

hawtio:
  authenticationEnabled: false

management:
  endpoints:
    web:
      exposure:
        #配置暴露端点
        include: hawtio,jolokia
        path-mapping:
          jolokia: medidas
      base-path: /

spring:
  datasource:
    url: jdbc:mysql://rds260kj5i2879nym9a9100.mysql.rds.aliyuncs.com:3306/tpa-saas?useSSL=false&autoReconnect=true&characterEncoding=utf8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: tpasaasuser
    password: G#EYff17

#    # 指定为HikariDataSource
#    type: com.zaxxer.hikari.HikariDataSource
#    # hikari连接池配置
#    hikari:
#      #连接池名
#      pool-name: HikariCP
#      #最小空闲连接数
#      minimum-idle: 5
#      # 空闲连接存活最大时间，默认10分钟
#      idle-timeout: 600000
#      # 连接池最大连接数，默认是10
#      maximum-pool-size: 10
#      # 此属性控制从池返回的连接的默认自动提交行为,默认值：true
#      auto-commit: true
#      # 此属性控制池中连接的最长生命周期，值0表示无限生命周期，默认30分钟
#      max-lifetime: 1800000
#      # 数据库连接超时时间,默认30秒
#      connection-timeout: 30000
#      # 连接测试query
#      connection-test-query: SELECT 1
  data:
    redis:
      host: 172.18.0.57  # 内网地址
      port: 6379 # 端口
      database: 0 # 数据库索引
      password: '@Pukang_123' # 密码，建议生产环境开启
      timeout: 10000
    jedis:
      pool:
        max-active: 50 # 连接池最大连接数（使用负值表示不限制连接池大小）
        max-idle: 10 # 连接池最大空闲连接数
        min-idle: 0 # 连接池最小空闲连接数
        max-wait: 30000 # 连接池获取连接最大等待时间，-1 表示永不超时，如果超过等待时间未获取到连接，将会抛出异常

eureka:
  client:
    serviceUrl:
      defaultZone: http://root:root@172.20.0.14:8761/eureka/

camel:
  cluster:
    id: dev