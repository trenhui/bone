# 采购订单元数据核心配置模板（通用优化版）
# 基于采购管理领域通用最佳实践，适配各类企业级采购系统，无特定平台依赖
# 版本：v1.0 | 适用场景：全行业中大型企业采购管理 | 兼容协议：UMP v4.0

apiName: "Procurement_PurchaseOrder"
label: "采购订单"
description: "企业级标准化采购订单，支持预算控制、供应商管理、多级审批、风险管控全流程"
domain: "procurement"
version: "1.0.0"
status: "ACTIVE"
tenantId: "default"
aiTag: "TEMPLATE_BASED"

# 继承配置（复用基础业务文档属性）
extends: "Base_BusinessDocument"
inheritanceType: "EXTENSION"
sharingModel: "PRIVATE"  # 数据隔离策略：私有（仅授权人员可见）
deploymentStatus: "DEPLOYED"

# 通用系统配置（适配各类采购系统）
systemConfig:
dataTracking: true  # 全字段变更历史跟踪
multiTenantIsolation: true  # 多租户数据隔离（集团型企业适用）
procurementCategories: ["GOODS", "SERVICES", "CAPITAL", "CONSULTING", "MATERIALS"]  # 全品类覆盖
approvalMatrix: "Dynamic_Approval_Matrix"  # 动态审批矩阵（按条件自动匹配）
budgetControlMode: "REAL_TIME"  # 预算控制模式：实时拦截
dataBackup: "DAILY"  # 数据备份频率：每日备份
logRetention: 365  # 操作日志保留时长：365天

# AI增强配置（可选启用，通用型适配）
aiEnhancement:
anomalyDetection: "REAL_TIME"  # 实时异常检测（异常价格、风险供应商、重复采购）
riskPrediction: "HIGH_PRECISION"  # 高精度风险预测（供应商履约风险、预算超支风险）
autoSuggest: true  # AI智能推荐（最优供应商、匹配预算科目、合理采购周期）
modelVersion: "procurement_llm_v4.0"  # 采购专用大模型版本
dataSource: ["historical_po", "market_price", "supplier_evaluation"]  # AI训练数据源

# 核心字段定义（通用型设计，适配各类系统）
fields:
# 1. 基础标识字段（全流程追溯核心）
orderNumber:
name: "orderNumber"
label: "订单编号"
type: "AUTO_NUMBER"
pattern: "PO-{YYYYMMDD}-{00000}"  # 格式：PO-20251020-00001（含精确日期，便于追溯）
required: true
unique: true
indexed: true  # 建立索引，提升查询效率
uiMetadata:
width: 200
readOnly: true
style: "font-weight: bold; color: #0f172a;"
bestPractices:
- "编号包含精确日期+流水号，支持按时间维度快速筛选"
- "全局唯一标识，确保全系统采购订单无重复"
- "索引优化，适配企业年采购量10万+场景"

# 2. 业务分类字段（精细化流程管控）
procurementType:
name: "procurementType"
label: "采购类型"
type: "PICKLIST"
required: true
defaultValue: "STANDARD"
picklistValues:
- value: "STANDARD"
label: "标准采购"
metadata:
approvalPolicy: "Standard_Approval"
sla: "7个工作日"
requiredFields: ["costCenter", "budgetLine", "deliveryAddress"]
applicableScenario: "常规生产/办公物资采购"
- value: "URGENT"
label: "紧急采购"
metadata:
approvalPolicy: "Urgent_Approval"
sla: "24小时"
requiredFields: ["urgentReason", "emergencyContact", "priorityLevel"]
applicableScenario: "生产断线、应急抢修等紧急场景"
- value: "CAPITAL"
label: "固定资产采购"
metadata:
approvalPolicy: "Capital_Approval"
sla: "14个工作日"
requiredFields: ["assetCategory", "depreciationTerm", "assetManager"]
applicableScenario: "设备、仪器、办公家具等固定资产"
- value: "SERVICE"
label: "服务采购"
metadata:
approvalPolicy: "Service_Approval"
sla: "10个工作日"
requiredFields: ["serviceScope", "servicePeriod", "acceptanceStandard"]
applicableScenario: "咨询、运维、外包等服务类采购"
uiMetadata:
width: 160
onChange: "dynamicRequiredFields()"  # 动态更新必填字段（根据采购类型切换）
tooltip: "选择采购类型后，系统将自动匹配对应审批流程与必填项"
bestPractices:
- "按采购场景细分类型，避免‘一刀切’流程"
- "动态控制必填字段，减少无效输入（如非紧急采购不显示‘紧急原因’）"
- "配置场景说明，降低一线人员操作门槛"

# 3. 供应商关联字段（风险前置管控）
supplier:
name: "supplier"
label: "供应商"
type: "LOOKUP"
targetEntity: "Procurement_Supplier"  # 关联供应商主数据
required: true
indexed: true
filterCondition: "IsActive = TRUE && QualificationStatus = 'QUALIFIED' && RiskLevel <= 'MEDIUM'"  # 仅显示合格低风险供应商
relationshipConfig:
relationshipType: "PURCHASES_FROM"
associatedFields: ["supplierCode", "contactPerson", "contactPhone", "bankAccount", "taxRegistrationNo"]  # 关联显示核心信息
historyTracking: true  # 跟踪该供应商历史采购记录
validation:
- expression: "${supplier.QualificationScope.contains(procurementType)}"
message: "供应商【${supplier.supplierName}】不具备【${procurementType}】采购资质（资质范围：${supplier.QualificationScope}）"
severity: "ERROR"
- expression: "${supplier.RiskScore <= 60}"
message: "供应商【${supplier.supplierName}】风险评分过高（当前：${supplier.RiskScore}/100，阈值：60），建议选择其他供应商"
severity: "WARNING"
- expression: "${supplier.DeliveryOnTimeRate >= 85}"
message: "供应商【${supplier.supplierName}】交货准时率较低（当前：${supplier.DeliveryOnTimeRate}%，阈值：85%），请确认是否继续"
severity: "WARNING"
uiMetadata:
lookupSearchFields: ["supplierCode", "supplierName", "contactPerson", "businessScope"]  # 多维度搜索
width: 240
showIcon: true
iconMapping:  # 按供应商等级显示不同图标
- condition: "${supplier.SupplierLevel == 'STRATEGIC'}"
icon: "strategic-supplier-icon"
- condition: "${supplier.SupplierLevel == 'CORE'}"
icon: "core-supplier-icon"
- condition: "${supplier.SupplierLevel == 'GENERAL'}"
icon: "general-supplier-icon"
bestPractices:
- "前置筛选合格供应商，从源头规避资质风险"
- "多维度验证（资质、风险、履约），降低采购风险"
- "关联显示核心信息，减少跨系统查询操作"

# 4. 预算控制字段（全流程预算管控）
budgetLine:
name: "budgetLine"
label: "预算科目"
type: "LOOKUP"
targetEntity: "Finance_BudgetLine"  # 关联财务预算主数据
required: true
indexed: true
filterCondition: "FiscalYear = CURRENT && BudgetStatus = 'ACTIVE' && AvailableAmount > 0"  # 仅显示当前财年有效且有余额的科目
budgetConfig:
budgetControl: "HARD_STOP"  # 硬性控制：超预算直接拦截
budgetReservation: true  # 订单提交后占用预算额度
budgetRecovery: "ORDER_CANCEL"  # 订单取消后释放预算
validation:
- expression: "${budgetLine.ValidPeriod.startDate <= requiredByDate && budgetLine.ValidPeriod.endDate >= requiredByDate}"
message: "预算科目【${budgetLine.budgetName}】在需求日期【${requiredByDate}】期间无效（有效期：${budgetLine.ValidPeriod.startDate}-${budgetLine.ValidPeriod.endDate}）"
severity: "ERROR"
- expression: "${budgetLine.AvailableAmount >= totalAmount}"
message: "预算科目【${budgetLine.budgetName}】余额不足（可用余额：${budgetLine.AvailableAmount}，订单金额：${totalAmount}）"
severity: "ERROR"
- expression: "${totalAmount <= budgetLine.SingleOrderLimit}"
message: "订单金额超过预算科目【${budgetLine.budgetName}】单笔限额（单笔限额：${budgetLine.SingleOrderLimit}，订单金额：${totalAmount}）"
severity: "ERROR"
uiMetadata:
lookupFilter: "Department = ${currentUser.Department} || IsShared = TRUE"  # 仅显示本部门及共享预算
width: 220
tooltip: "选择预算科目后，系统将实时校验余额与限额"
bestPractices:
- "实时硬性预算控制，杜绝超预算采购"
- "按部门过滤预算科目，避免跨部门错用预算"
- "配置预算回收机制，确保预算使用准确性"

# 5. 金额计算字段（自动精准计算）
totalAmount:
name: "totalAmount"
label: "订单总额(含税)"
type: "CURRENCY"
precision: 19
scale: 2
calculated: true  # 自动计算，不允许手动输入
calculationExpression: >
${items.?[status=='ACTIVE'].![quantity * unitPrice * (1 + taxRate/100) - discountAmount].sum() ?: 0}
# 计算逻辑：有效明细行（数量×单价×(1+税率) - 折扣金额）求和
indexed: true
currencyConfig:
defaultCurrency: "${currentCompany.DefaultCurrency}"  # 默认币种：企业本位币
multiCurrencySupport: true  # 支持多币种（明细行可填外币，自动折算本位币）
exchangeRateSource: "OFFICIAL_DAILY"  # 汇率来源：官方每日更新
aiEnhancement:
anomalyDetection: true  # 价格异常检测
priceThreshold: "${getMarketPriceThreshold(procurementType, items.category)}"  # 动态阈值（按采购类型+品类）
priceComparison: ["HISTORICAL_PO", "MARKET_PRICE", "SUPPLIER_QUOTATION"]  # 多维度比价（历史订单、市场价、供应商报价）
abnormalAction: "ALERT + APPROVAL_UPGRADE"  # 异常处理：触发告警+升级审批
uiMetadata:
format: "currency"
readOnly: true
style: "font-weight: bold; color: #1e40af;"
width: 180
suffix: "（${currentCompany.DefaultCurrency}）"
bestPractices:
- "自动计算金额，避免手动输入错误（如税额漏算、折扣错填）"
- "支持多币种折算，适配跨国采购场景"
- "AI价格异常检测，控制采购成本偏差（建议偏差阈值±15%）"

# 6. 时间规划字段（履约周期管控）
requiredByDate:
name: "requiredByDate"
label: "需求日期"
type: "DATE"
required: true
validation:
- expression: "${requiredByDate >= new Date() && requiredByDate <= new Date(System.currentTimeMillis() + 90*24*60*60*1000)}"
message: "需求日期需在今日起90天内（当前系统日期：${new Date()}）"
severity: "ERROR"
- expression: "${requiredByDate >= getSupplierDeliveryPeriod(supplier, items.category)}"
message: "需求日期早于供应商最短交付周期（供应商【${supplier.supplierName}】${items.category}品类最短交付周期：${getSupplierDeliveryPeriod(supplier, items.category)}天）"
severity: "WARNING"
uiMetadata:
width: 160
datePickerType: "date"
disabledDates: ["PAST"]  # 禁用过去日期
recommendedDate: "${getRecommendedDeliveryDate(supplier, items.category)}"  # AI推荐合理交付日期
bestPractices:
- "限制需求日期范围，避免不合理规划（如当天需求无法满足）"
- "关联供应商交付周期，确保需求日期可行性"
- "提供推荐日期，降低一线人员判断成本"

# 7. 紧急采购补充字段（条件显示，减少冗余）
urgentReason:
name: "urgentReason"
label: "紧急原因"
type: "TEXTAREA"
required: false  # 动态控制：仅紧急采购时必填
maxLength: 500
validation:
- expression: "${procurementType == 'URGENT' ? urgentReason.length >= 50 : true}"
message: "紧急原因需至少50字，说明紧急场景（如生产断线原因、影响范围、预期损失等）"
severity: "ERROR"
uiMetadata:
width: 450
rows: 4
showCondition: "${procurementType == 'URGENT'}"  # 仅紧急采购显示
placeholder: "请详细说明紧急原因，包括但不限于：紧急场景、影响范围、预期损失、是否有替代方案等"
bestPractices:
- "条件显示字段，避免正常采购流程冗余"
- "限制最小字数，确保紧急原因真实可追溯"
- "提供填写指引，规范紧急原因描述"

# 8. 履约管控字段（降低交付风险）
deliveryAddress:
name: "deliveryAddress"
label: "交付地址"
type: "LOOKUP"
targetEntity: "Org_Address"  # 关联企业组织地址库
required: true
filterCondition: "AddressType = 'DELIVERY' && IsActive = TRUE"  # 仅显示有效交付地址
uiMetadata:
width: 300
lookupSearchFields: ["addressName", "province", "city", "detailAddress"]
showFullAddress: true  # 显示完整地址（省+市+详细地址）
bestPractices:
- "关联标准地址库，避免地址错填"
- "支持多维度搜索，提升地址选择效率"

# 9. 验收管控字段（确保采购质量）
acceptanceStandard:
name: "acceptanceStandard"
label: "验收标准"
type: "TEXTAREA"
required: true
maxLength: 1000
defaultValue: "${getDefaultAcceptanceStandard(items.category)}"  # 按品类填充默认验收标准
uiMetadata:
width: 450
rows: 5
placeholder: "请填写验收标准，包括：数量验收、质量标准、规格参数、验收方式、不合格处理方式等"
showCondition: "${procurementType != 'URGENT' || totalAmount > 10000}"  # 紧急采购且金额超1万需填
bestPractices:
- "按品类提供默认验收标准，减少重复填写"
- "大额/非紧急采购强制填写，确保质量可控"

# 业务规则配置（全流程合规管控）
businessRules:
# 1. 预算全流程验证规则（财务合规核心）
- name: "budget_validation"
  label: "预算全流程验证"
  category: "FINANCIAL_CONTROL"
  active: true
  condition: "${totalAmount > 0 && budgetLine != null && requiredByDate != null}"
  expression: >
  ${checkBudgetAvailability(
  budgetLine.budgetId,
  totalAmount,
  requiredByDate,
  currentFiscalYear,
  budgetTolerance = 0  # 预算容忍度：0（无容忍，超预算直接拦截）
  ) && checkBudgetPeriod(budgetLine.validPeriod, requiredByDate) && checkSingleOrderLimit(budgetLine.singleOrderLimit, totalAmount)}
  errorMessage: >
  预算验证失败：
    1. 预算科目【${budgetLine.bud</doubaocanvas>