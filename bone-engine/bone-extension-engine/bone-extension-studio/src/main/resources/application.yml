server:
  port: ${BONE_EXTENSION_STUDIO_PORT:8088}
  servlet:
    context-path: /

spring:
  application:
    name: bone-extension-studio
  profiles:
    default: in-memory
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 55MB
  # 数据库配置（metadata profile 会覆盖 url 并执行 schema-h2.sql）
  datasource:
    url: jdbc:h2:mem:extension-studio;DB_CLOSE_DELAY=-1
    driverClassName: org.h2.Driver
    username: sa
    password: 

  # H2控制台（仅开发环境使用，生产环境应禁用）
  h2:
    console:
      enabled: ${H2_CONSOLE_ENABLED:false}
      path: /h2-console
      settings:
        web-allow-others: false

# 与 bone-iam 对齐的 JWT（生产环境请外部化 secretKey）
bone:
  iam:
    jwt:
      secret-key: ${BONE_IAM_JWT_SECRET_KEY:${BONE_JWT_SECRET:dev-only-secret-key-minimum-32-bytes-long}}
      header-name: Authorization
      token-prefix: "Bearer "
  extension:
    enabled: false
    studio:
      runtime-sync:
        enabled: true
      artifact:
        storage-path: ${BONE_EXTENSION_ARTIFACT_PATH:./data/extension-plugins}
        max-versions-per-plugin: 3
      # 扩展点扫描配置
      scan:
        enabled: true
        base-packages: com.bone.engine.extension.example
      
      # 缓存配置
      cache:
        enabled: true
        ttl: 3600

# 安全配置
security:
  basic:
    enabled: false
  
# 日志配置
logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] [%X{traceId:-}] %-5level %logger{36} - %msg%n"
  level:
    root: INFO
    com.bone.engine.extension.studio: DEBUG

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
    tags-sorter: alpha
    operations-sorter: alpha

# 管理端点配置
management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      show-details: always