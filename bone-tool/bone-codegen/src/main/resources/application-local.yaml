--- #################### 数据库相关配置 ####################
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/infra?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: ${BONE_DB_PASSWORD:}

metadata:
  sdk:
    appcode: tpa
    open: true
    secret: ${METADATA_SDK_SECRET:}
    sqlpath: testsql/
    url: http://metabata.dev6.pukangbao.com