/**
 * ${className} 列表页面入口
 * 集成了ProTable列表 + 弹窗新增编辑
 *遵循 Ant Design Pro 最佳实践
 */
import React from 'react';
import ${className}List from './../${className}/List';

/**
 * ${className} 页面主组件
 */
export default function ${className}Page() {
  return <${className}List />;
}
