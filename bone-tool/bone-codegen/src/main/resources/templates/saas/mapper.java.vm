package $packageName.infrastructure.mapper;

import com.baomidou.mybatisplus.core.conditions.Wrapper;
import com.baomidou.mybatisplus.core.mapper.BaseMapper;
import com.baomidou.mybatisplus.core.toolkit.Constants;
import org.apache.ibatis.annotations.Param;
import $packageName.domain.entity.$className;
import com.bone.metadata.sdk.query.criteria.Criteria;
import java.util.List;
import java.util.Map;
import java.util.Collection;

/**
 * $classCommentMapper接口
 * 提供$classComment相关的数据库操作
 * @author $author
 * @date $datetime
 */
public interface ${className}Mapper extends BaseMapper<$className> {

    /**
     * 根据条件查询
     * @param criteria 查询条件
     * @return 查询结果列表
     */
    List<$className> selectByCriteria(Criteria criteria);

    /**
     * 批量插入
     * @param list 数据列表
     * @return 插入行数
     */
    int batchInsert(@Param("list") List<$className> list);

    /**
     * 批量更新
     * @param list 数据列表
     * @return 更新行数
     */
    int batchUpdate(@Param("list") List<$className> list);

    /**
     * 根据ID集合查询
     * @param ids ID集合
     * @return 查询结果列表
     */
    List<$className> selectByIds(@Param("ids") Collection<Long> ids);

    /**
     * 根据条件查询单个结果
     * @param criteria 查询条件
     * @return 查询结果
     */
    $className selectOneByCriteria(Criteria criteria);

    /**
     * 统计符合条件的记录数
     * @param criteria 查询条件
     * @return 记录数
     */
    Long countByCriteria(Criteria criteria);

    /**
     * 批量删除
     * @param ids ID集合
     * @return 删除行数
     */
    int deleteBatchIds(@Param("ids") Collection<Long> ids);

    /**
     * 根据条件分页查询
     * @param criteria 查询条件
     * @param offset 偏移量
     * @param limit 限制数
     * @return 查询结果列表
     */
    List<$className> selectPageByCriteria(Criteria criteria, 
                                        @Param("offset") int offset, 
                                        @Param("limit") int limit);

    /**
     * 动态查询
     * @param queryWrapper 查询条件包装器
     * @return 查询结果列表
     */
    List<$className> selectList(@Param(Constants.WRAPPER) Wrapper<$className> queryWrapper);

    /**
     * 根据Map条件查询
     * @param columnMap 查询条件Map
     * @return 查询结果列表
     */
    List<$className> selectByMap(@Param("cm") Map<String, Object> columnMap);

    /**
     * 根据条件更新（非空字段）
     * @param entity 实体对象
     * @param updateWrapper 更新条件包装器
     * @return 更新行数
     */
    int update(@Param(Constants.ENTITY) $className entity, 
              @Param(Constants.WRAPPER) Wrapper<$className> updateWrapper);
}