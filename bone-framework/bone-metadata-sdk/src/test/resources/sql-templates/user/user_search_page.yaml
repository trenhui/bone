id: user_search_page
sql: |
  SELECT u.id, u.name, r.role_name AS role
  FROM users u
  LEFT JOIN roles r ON u.role_id = r.id
  <where>
    <if test="username != null">
      AND LOWER(u.name) = LOWER(:username)
    </if>
  </where>
mappedClassName: com.bone.metadata.sdk.test.domain.dto.UserRoleDTO